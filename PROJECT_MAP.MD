# Vikings Strategy Game - Project Map

## Overview
Turn-based strategy game in Godot 4.3 with procedural map, resources, recruitment, movement, and battles for up to 6 players. Architecture emphasizes separation of concerns and SRP.
- Localization: gameplay UI/runtime messages in scripts use `tr(...)`; enum display text should use display/localized helper APIs (canonical enum string methods remain for data/serialization).
- AI behavior reference: `AI_BEHAVIOUR.md` documents current AI turn economy flow, target scoring, and movement/battle decision logic.
- Castle threat reference: `CASTLE_THREAT_LOGIC.md` documents runtime castle-threat detection/classification and per-army threat response branches.
- Player guide reference: `GAME_GUIDE.md` provides player-facing explanations of economy, recruitment, battles, sieges, and core strategy.
- Analytics: `Analytics` autoload (`analytics.gd`) sends queued app heartbeat events plus content-run analytics (`run_start`, `run_heartbeat`, `run_finish`, `run_event`) to `heartbeat.php`; payloads include `run_source`, `is_internal`, and `internal_reason` so Godot/editor or marked internal-device runs can be excluded later; `analytics_schema.sql` defines additive MySQL tables for raw events and run summaries.
- Analytics dashboard: `analytics-dashboard/` is a standalone React + Node/MySQL dashboard for `analytics_runs` and `analytics_events`; it summarizes run volume, unique players, completion/quit/win rates, average duration/turns, content performance, difficulty/status/platform breakdowns, event volume, and recent runs with time-period and metadata filters. Dashboard data excludes internal rows and `.env` configured owner/test `user_id`s by default.
- Rendering/runtime defaults: project settings enforce VSync (`window/vsync/vsync_mode=enabled`) for frame pacing; 2D pixel snap is disabled to avoid zoom-time jitter and stepped camera/cloud motion, while clouds use per-sprite `Linear with Mipmaps` filtering.
- Window mode: `WindowModeManager` autoload toggles fullscreen/windowed globally with `Alt+Enter` or `F11`; windowed mode opens at 1600x900, remains resizable, and remembers the last windowed size during the current run.

## Scenes

### splash_screen.tscn
- Startup scene (project `run/main_scene`) shown before main menu.
- Demo-only splash flow: when `GameParameters.DEMO_MODE_ENABLED` is `true`, displays `images/splash.png`, fades it in from alpha `0` to `1` over `1.0s`, holds for another `1.0s`, then transitions to `scenes/main_menu.tscn`; when demo mode is `false`, immediately transitions to main menu (no splash display).
- Godot built-in boot splash image is disabled (`application/boot_splash/show_image=false`) so only this custom splash is visible.

### main_menu.tscn
- Entry UI. Starts a new game, tutorial, or continues from save; loads `main.tscn`.
- Runtime localization loads both `hotw_translations.*.translation` and `missions.*.translation` from project internationalization settings, so scenario keys like `mission-1_intro`/`mission-1_description` resolve via `tr(...)`.
- Main menu locale startup order is: saved settings locale (`SaveGameManager.get_saved_locale()`) -> Steam language (when `Steam` singleton is available) -> OS locale -> `en`; locale is normalized to supported set (`en`, `de`, `pl`, `br`), and manual language switching still persists via settings.
- Main menu button theme (`themes/main_menu_theme.tres`) uses empty disabled style for primary menu buttons.
- Map preview panel (`MapPreview/InnerPanel/MapScreenshot`) handles campaign/scenario hovers; a separate CustomMap preview lives in `CustomMap/Panel2/TextureRect` and scales previews to a 450x450 frame (keep-aspect-covered, fit width).
- Custom Map & Scenario submenus share `CustomMap`:
  - Panel (players/difficulty/victory + Select Map) shows for custom maps; Scenario panel shows for scenario list; both Back buttons return to New Game. Custom Map/Scenario/Campaign Back and Select/Start action buttons use the green button theme.
  - Player toggles are static scene nodes with defaults: P1 Human, P2-4 Computer, P5-6 Off; difficulty defaults to Normal; custom-map victory defaults to `Conquer`; selection uses default button styling (no font color override). Map Settings player-name labels use `GameParameters.PLAYER_COLORS` as base, with a slight readability lift for P3 and P6 on the dark background.
  - New game start payload now carries `difficulty` (`easy|normal|hard`) from Custom Map/Scenario selectors; tutorial payloads default to `normal`.
  - Map/Scenario list (Panel3) uses a scrollable template row (`Map1`) sorted by size (XS/S/M/L) then name; Custom Map/skirmish entries only include map JSON files with a top-level `display_name_key` and use that key for display/sorting instead of filename-derived names; size filter buttons (All/XS/S/M/L) rebuild the list and select the first available row by default; Up/Down cursor keys move selection within the visible list; row hover/unhover callbacks are wired to duplicated `MapListRow` (`HBoxContainer`) entries. Size buckets are now derived from map profile resolution (`filename token + map JSON regions count`) instead of suffix-only parsing.
  - Map/scenario size filtering and sorting now normalize legacy/new size aliases (`T`, `tiny`, `xtiny`, `xxtiny`) to `XS`, so `XS` and `All` filters include those entries consistently.
  - Scenario/custom-map list size badges now resolve from actual map JSON `regions.size()` through `Utils.get_frontend_size_code_from_region_count(...)` (with token-based fallback only when region data is unavailable).
  - Resetting map/scenario selection no longer mutates cached list item dictionaries, so refiltering does not drop the previously selected scenario row.
  - Selecting a row keeps label font colors at their default (same as unselected) and highlights the full row with a semi-transparent black background; hover uses a lighter black background. Selection still updates Panel2 labels (MapName = localized display name, with the real map filename appended for Custom Map rows only when main-game `debug_mode` is enabled; MapSize = Extra Small/Small/Medium/Large), enables the Select button (text switches to "Start Game"), and loads the preview if available under `previews/<map_or_scenario>.png`; for Scenario/Campaign rows preview lookup uses the scenario JSON filename base, while Custom Map rows use the map filename base. Custom Map, Scenario, and Campaign lists auto-select the first newly created row after list build/filter. Map/scenario/campaign row names resolve from translation keys first (when provided), then fall back to `tr(display_name)`.
  - List selection now clears stale row references before recoloring so reopening lists after Back does not hit freed-node crashes.
  - Campaign button now opens the Scenario panel with "Campaign" header, hides Panel3 and shows Panel4 (campaign list without size filters), and lists only scenarios with JSON `scenario_type = "campaign"`; Scenario button lists `scenario_type = "scenario"` and defaults missing `scenario_type` to scenario. Campaign list entries use scenario names (and existing translation-key resolution when provided), not `Mission <number>` labels, campaign ordering is by JSON `mission_number` ascending, and the list left column shows `mission_number` instead of map size. Mission 1 and Mission 2 are always selectable; later campaign missions stay visible but disabled until the previous mission number has been completed. Main menu also consumes optional `SceneTree` meta `main_menu_target = "campaign_list"` to open directly into the campaign list view.
  - Scenario/Campaign difficulty buttons are mutually exclusive via a shared ButtonGroup (Normal preselected); scripted scenario difficulty selection updates both the visual button state and the pressed ButtonGroup state so the start payload records the displayed difficulty.
  - Hovering any main-menu difficulty button (Custom Map + Scenario/Campaign) now shows `CustomTooltip` with localized descriptions (`difficulty_easy_description`, `difficulty_normal_description`, `difficulty_hard_description`); hovering Custom Map victory buttons shows localized victory descriptions (`conquer_condition_description`, `dominate_condition_description`).
  - Scenario/campaign JSON can optionally set `difficulty` as `all|easy|normal|hard` (missing defaults to `all` for backward compatibility). In Scenario/Campaign selection, `all` keeps all difficulty buttons enabled; fixed values auto-select and lock to that difficulty by disabling the other options. Mission 1 is locked to Easy while using the previous Normal resources and army setup through its Easy override.
  - Scenario side panel now fills `Scenario Description` and `Victory conditions` text from selected scenario JSON keys `description` and `objectives` (falls back to panel defaults when empty), and applies `tr(...)` when displaying; description uses a fixed-height scrollable `RichTextLabel` so long text can be scrolled without changing panel layout.
- Menu UX: No click sounds on button presses in main menu; Esc routes to the active Back behavior for New Game, Options, Load Game, Scenario/Campaign, and Custom Map submenus.
- Options button now opens the shared `Options` panel (`scenes/options.tscn`) instead of the old inline submenu; Back returns to Main Menu and uses the green button theme.
- Embedded main-menu `Options` panel geometry now matches the in-game/shared options modal texture sizing (same background scale/height for the main panel and tab content containers).
- Language widget (`Language`) in main menu cycles locales with left/right arrows (`English`, `Deutsch`, `Polski`, `Português (Brasil)`), updates label + flag, applies `TranslationServer` locale, and persists locale in `user://settings.cfg`.
- Main menu primary button is dynamic: when no valid save files exist in `user://` it shows `Tutorial` and starts `res://scenarios/tutorial.json`; once at least one save exists it switches to `Continue` and starts `main.tscn` with `start_payload.type = "save"` using the most recently modified save file.
- Main menu has a bottom `Feedback` button that minimizes the window and opens `https://steamcommunity.com/app/4380120/discussions/0/` in the default browser; visible on the main menu and hidden in submenus.
- `Bonus Cards` opens the read-only `Cards` screen; before non-tutorial scenario/campaign/custom-map starts, the same screen enters selection mode with `Continue` and passes `selected_upgrade_cards` in `start_payload`, skipping this step when no cards are unlocked. Selection caps are Easy 3 cards, Normal 2 cards, Hard 1 card. `Cards` uses a `GridContainer` with 6 columns and 12 `Card#` instances of `scenes/upgrade_card.tscn`; Cards 1-5 are green resource cards (Food/Wood/Stone/Iron/Gold), Cards 6-12 are blue unit cards (Peasants/Spearmen/Archers/Swordsmen/Crossbowmen/Horsemen/Knights), and textures/level icons are refreshed from `UpgradesManager` progress. Card descriptions use translation keys with `{amount}` filled from the current unlocked card level. Hovering `Level1..3` shows a no-resource tooltip with the exact per-level requirement, including tutorial completion, total qualified wins, scenario/mission difficulty, or skirmish difficulty. Locked cards hide icon/text and show their `Lock` texture; view mode hides the selection-limit label and shows an empty spacer label to preserve layout; the Back/Continue button uses the green button theme. `TextureRect6` is the sixth main-menu button background and is visible only on the first-level main menu.
- Custom-map/skirmish starts with zero or multiple human players skip the pre-game `Cards` selection screen and start without selected upgrade cards.
- `Load Game` in main menu opens `SaveGameModal` in `LOAD` mode; selecting a row starts `main.tscn` with `start_payload.type = "save"` and `save_path` for the selected save file, and `Back` from this modal always restores the main menu view. Main-menu load modal Back and Load Game buttons use the green button theme.
- Save/load modal is split by context:
  - In-game: `scenes/save_game_modal.tscn` + `save_game_modal.gd` (used by `GameMenuModal`).
  - Main menu: embedded `SaveGameModal` node tree in `scenes/main_menu.tscn` with dedicated script `main_menu_save_game_modal.gd`.
  - Both variants keep the same visual structure (header/back/list/action), show `File Name/Type/Date`, use `FileNameInput.max_length = 20`, expand `ScrollContainer` + `ArmyTexture` by 72px in load mode, and use `mouse_filter = STOP` to block click-through.
  - Save/load list rows in both variants keep white label text for selected/unselected states and use a semi-transparent black full-row background for hover/selection.
- Saves persist `game_state.game_difficulty` so loaded scenario/campaign/skirmish games keep the originally selected difficulty for runtime systems and victory upgrade unlock recording; older saves without the key default through existing runtime difficulty.
- Main-menu decorative TextureRects (`Background`, `TextureRect1..5`) use `mouse_filter = IGNORE` so they cannot steal clicks from active option controls.
- Demo mode now reuses the standard main menu flow and limits selectable content to an allowlist: scenarios/campaign entries `mission-1.json`, `mission-2.json`, `tutorial.json`, `Vikings Invasion.json`, and custom map entry `mapdata_287_401.json`; in custom-map player settings, Player 5 and Player 6 are locked to `Off` by disabling their `Player/Computer` toggles.

Start Flow
- Main menu sets `SceneTree` meta `start_payload` and loads `main.tscn`:
  - Scenario: `{ type: "scenario", scenario_path: res://scenarios/<name>.json }`
- Custom map: `{ type: "map", map_file: res://mapdata/<file>.json, map_size: <tiny|small|medium|large|huge>, player_settings: [...], victory_condition: <""|conquer|dominate> }`.
- Saved game: `{ type: "save", save_path: user://<save_name>.json }` (path chosen from available save files; default save path remains `user://savegame.json`).
- `GameManager._ready()` reads `start_payload`:
  - Scenario: sets `game_mode = "scenario"`, assigns `scenario_path` (pre-load applies scenario map file) and loads scenario-defined `victory_conditions` (missing/empty means no automatic victory).
  - Custom map: sets `game_mode = "custom"`, assigns `MapGenerator.data_file_path`, applies compatibility `map_size` via `_map_set_size_from_string`, applies player_settings if present, reads `victory_condition`, then `generate_map()`.
  - For scenario/custom starts, reads `difficulty` and stores it as `GameManager.game_difficulty` (single runtime source for difficulty-dependent systems).
  - For scenario/custom starts, optional `selected_upgrade_cards` are validated against unlock progress and applied once to human starting resources/unit targets; tutorial/save starts do not receive fresh upgrade bonuses.
  - Save: loads the path from payload (`save_path`), regenerates map from saved source (`map_file` + compatibility `map_size`), initializes managers without castle-placement bootstrap, then applies full runtime state (regions/ownership/castles/armies/player/game turn state) and resumes saved analytics run state when present.
  - Payload is cleared after consumption.
- Custom map start shows `MessageModal` prompt "Click a region to choose your starting location" during castle placement only when at least one human player is active.

### main.tscn (Primary)
Main (Node2D) [place_armies.gd]
├ Map (Node2D) [map_generator.gd] – precomputes `Region.nearby_regions` (depth 2 BFS over non-ocean, non-mountain adjacency) on map generation; resolves per-map profile from `filename token + regions.size()` (`region_count`, visual scale, initial zoom, frontend size bucket); normalizes mapdata regions with biome `lake` into ocean regions at load (`ocean=true`, `water=true`, `biome="ocean"`).
│	├ Clouds (Node2D) [clouds.gd] – spawns clouds (cloud1.png..cloud5.png) with matching shadows, random scale/position within map size, moves left-to-right at per-cloud speed, respawns at x=-1000 after crossing x=3050; cloud and shadow sprites force `texture_filter = Linear with Mipmaps` for cross-platform motion stability; exposes global/runtime cloud visibility (`set_global_clouds_enabled`, `set_clouds_enabled`) used by the shared Options panel; shadow blur uses `cloud_shadow_blur.gdshader`
│	├ Regions (Node2D) – dynamic `Region` nodes [region.gd] named `<region_name>#<region_id>` (land polygons tile `background4grass.png` and still rebuild from the stored noisy border points; ocean polygons remain hidden by default)
│	│	└ Borders: `BorderManager` instantiates named Line2D nodes (`OceanBorder@id`, `OwnershipBorder@id`, `InternalBorder@id`) and only adjusts width/color/offset when ownership changes so counts always match adjacency.
│	├ Ocean (Node2D) – metadata-only ocean polygons (hidden visuals)
│	├ Frame (Node2D)
│	├ BorderManager (Node) [border_manager.gd] – centralizes noisy border creation, caches per-edge geometries, preserves ocean strokes (5.2px black) and exposes refresh helpers consumed by MapGenerator/RegionManager; MapEditor now reruns `BorderManager.setup()` (plus the noisy polygon rebuild/overlay sync) whenever a region toggles between land and ocean so the new coastline/borders appear immediately.
│	└ TutorialWorldArrow (Sprite2D) – single world-space tutorial arrow used for region-anchored hints
├ Background (Sprite2D)
├ Camera2D [camera_controller.gd] – handles pan/zoom; shift+left-drag or right-drag pans; wheel zoom; Q/E zoom; R reset (blocked when pan is blocked by modal UI); emits camera_moved_by_player and camera_zoomed_by_player during tutorial. Pan/zoom smoothing lerp weights are clamped to `[0,1]` to avoid frame-time extrapolation jitter on slower/variable FPS platforms. Zoom inputs (wheel, Q/E, magnify gesture) are blocked while modal UI is active, except when the only visible modal set is `InfoModal` and/or `MessageModal`; tutorial step `camera_zoom` keeps zoom enabled with tutorial message modal visible. Pan inputs (trackpad pan gesture, drag pan, keyboard pan, touch-drag pan) are blocked while modal UI is active, except when only `InfoModal` is visible; tutorial steps `camera_move` and `camera_zoom` keep pan enabled with tutorial message modal visible.
├ ClickManager (Node) [click_manager.gd]
├ GameManager (Node) [game_manager.gd]
├ SoundManager (Node) [sound_manager.gd]
├ PlayerManager (Node) [PlayerManagerNode.gd]
├ MapEditor (Node) [map_editor.gd]
├ UI (CanvasLayer)
│	├ UIManager (Control) [ui_manager.gd] – tracks modal visibility list to drive `is_modal_active`; IconsModal hides for blocking overlays and active move selection but stays visible when only InfoModal (region view) is open; TurnModal follows modal/overlay suppression rules and stays available during army move selection; owns tooltip coordination (normal region hover tooltips plus move-flow handoff so region tooltip can show together with move cost; move-flow tooltip remains visible while move selection is active, is forced to front, and is anchored to the position provided by ClickManager with a small tuned offset); ClickManager/UIManager now block map clicks from dismissing Prebattle/Battle/BattleSummary modals (buttons only). While scenario SpawnEvent manual placement mode is active, ESC is consumed (hard-block) so modal closing cannot interrupt placement.
│	├ ArmyModal [scenes/army_modal.tscn] [army_modal.gd]
│	├ SelectModal [scenes/select_modal.tscn] [select_modal.gd]
│	├ RegionSelectModal [scenes/region_select_modal.tscn] [region_select_modal.gd] – action-list region modal used by `GeneralSelectModal`; promote/build/repair/ore-search/raise-army resource checks and spends resolve against the current turn player.
│	├ GameMenuModal [scenes/game_menu_modal.tscn] [game_menu_modal.gd] – pause overlay using select-modal styling; 62px tall buttons matching other select modals and ordered as `Continue`, `Options`, `Load Game`, `Save Game`, `Game Guide`, `Exit Game`; `Load Game` opens `SaveGameModal` in load mode, `Save Game` opens it in save mode, `Game Guide` opens `GameGuideMenuModal` (section submenu), and `Exit Game` returns to `main_menu.tscn`; Options opens shared `Options` panel (`scenes/options.tscn`) and Back returns to the pause menu; while visible (including paused state), `Esc` closes the menu; during tutorial (`GameManager.tutorial_enabled`) `Load Game` and `Save Game` are disabled; disabled rows use gray text and darkened button backgrounds; modal layering is ordered so `GameMenuModal` is above `MessageModal`, and `Options`/`SaveGameModal` are above `GameMenuModal`
│	├ GameGuideMenuModal [scenes/game_guide_menu_modal.tscn] [game_guide_menu_modal.gd] – game-guide submenu (copied game-menu style) with `Back`, `Region`, `Economy`, `Recruitment`, `Battles`, `Tips`; opens `GameGuideModal` for the selected section and restores itself after `Continue`; `Back` returns to `GameMenuModal`
│	├ Options [scenes/options.tscn] [options.gd] – shared options window used by both main menu and in-game pause menu; controls bus-backed sound/music volumes through `SoundManager`, cloud show/hide via `clouds.gd`, plus `AI Turn Speed` and `Battle Speed` (`Normal/Fast/Very Fast`) bound to the same `GameParameters` values used by `SpeedModal`; includes `Move army` trigger (`Left click`/`Right click`) and `Keyboard mapping` configuration (`Continue/Close`, `Next army`, `Switch army/region`, `Recruit`, `Camp/Rest`, `Transfer`) persisted in settings and applied globally; move modal keyboard actions use these mappings.
│	├ InfoModal [scenes/info_modal.tscn] [info_modal.gd]
│	│	└ Uses `themes/standard_text_modal_theme.tres` for body text (#f1d891) and `themes/header_modal_theme.tres` for headers; Region/Armies tabs toggle Body panels + RegionTextures/ArmyTextures, update hover cursor + inactive label color, and Tab key cycles tabs; active tab is preserved across show calls (region/army updates follow the active tab); Armies tab renders Army1-5 cards per region (MP, vigor, composition, selection status) with hover panel texture/label color and auto-selects the first army with MP>0 (clicking a card selects on-map and opens MoveModal); RegionPanel drives region header, level/promotion cost (including gold), castle defense + build/repair cost (gold/wood/stone/iron), mine status + cost (Mine ActionSection hidden when no ore potential or searches are exhausted), raise army name + cost, garrison/recruits (pooled from owned neighbors) plus Replenish preview (+x.x/Turn base rate, rounded down to 0.1; carry-over decimals are internal-only; non-castle = current region only, castle = owned neighbors pool), defenders, and resources (zero-cost resources hide); while castle construction is in progress, Castle action resources hide Gold/Wood/Stone/Iron and show only Time (`time.png`) with remaining turns; region actions (promote/build/repair/ore-search/raise-army) always validate and spend resources for the current turn player (not hardcoded player 1); successful promotion triggers `SoundManager.play_promote_sound()` and uses a single localized promotion message format (`Region promoted to {level}\n(level {level_number})`, translated via the escaped-newline key and rendered with a real line break), raise-army success triggers `SoundManager.play_horn_sound()` and uses localized army label format (`Army %s`) in the raise message, and castle build/upgrade actions trigger `SoundManager.play_hammer_sound(3.0)`; ore search plays `SoundManager.play_mining_sound()` on success and `SoundManager.play_mining_sound(2.0)` on failed search (50% volume); InfoModal action/selection buttons use non-toggle mode so pressed highlight never latches (hover-only feedback), RegionLevel ActionSection is hidden at max region level and CastleLevel ActionSection is hidden at max castle level, with Build/Upgrade using `SelectTooltipModalUpkeep` for castle upkeep + build time, Promote using the upkeep tooltip from region level 2 onward, and other actions using message-only tooltips; garrison unit icon hover (Region defenders + Armies cards) reuses `SelectTooltipModalNoRes` to show localized unit names with fixed ActionTooltip X and garrison-row Y; actions are hard-disabled for the rest of the turn when `current_region.just_conquered_this_turn` is true (tooltip key: `conquered_region_blocked`); enemy/neutral regions use intel mode in non-debug view (hide action sections, keep resources visible, show last-known garrison/army composition, show army intel age in turns, and block enemy army selection), and returning to owned regions restores Raise Army/Garrison action sections; Armies-tab tutorial UI click events are sent only when tutorial mode is enabled; during scenario SpawnEvent placement mode it can be locked to Armies-only (Region tab + TAB switching disabled temporarily); emits `closed_info_modal` when the panel transitions from visible to hidden
│	│	└ Uses `themes/standard_text_modal_theme.tres` for body text (#f1d891) and `themes/header_modal_theme.tres` for headers; Region/Armies tabs toggle Body panels + RegionTextures/ArmyTextures, update hover cursor + inactive label color, and Tab key cycles tabs; active tab is preserved across show calls (region/army updates follow the active tab); Armies tab renders Army1-5 cards per region (MP, vigor, composition, selection status) with hover panel texture/label color and auto-selects the first army with MP>0 (clicking a card selects on-map and opens MoveModal); RegionPanel drives region header, level/promotion cost (including gold), castle defense + build/repair cost (gold/wood/stone/iron), mine status + cost (Mine ActionSection hidden when no ore potential or searches are exhausted), raise army name + cost, garrison/recruits (pooled from owned neighbors) plus Replenish preview (+x.x/Turn base rate, rounded down to 0.1; carry-over decimals are internal-only; non-castle = current region only, castle = owned neighbors pool), defenders, and resources (zero-cost resources hide); region income/resource values in RegionResources now render with a `+` prefix for positive values only (`0` stays `0`, population excluded), and Promote hover shows predicted income/resource and population-growth deltas in PromotionBonus labels using displayed rounded before/after growth values; while castle construction is in progress, Castle action resources hide Gold/Wood/Stone/Iron and show only Time (`time.png`) with remaining turns; region actions (promote/build/repair/ore-search/raise-army) always validate and spend resources for the current turn player (not hardcoded player 1); successful promotion triggers `SoundManager.play_promote_sound()` and uses a single localized promotion message format (`Region promoted to {level}\n(level {level_number})`, translated via the escaped-newline key and rendered with a real line break), raise-army success triggers `SoundManager.play_horn_sound()` and uses localized army label format (`Army %s`) in the raise message, and castle build/upgrade actions trigger `SoundManager.play_hammer_sound(3.0)`; ore search plays `SoundManager.play_mining_sound()` on success and `SoundManager.play_mining_sound(2.0)` on failed search (50% volume); InfoModal action/selection buttons use non-toggle mode so pressed highlight never latches (hover-only feedback), RegionLevel ActionSection is hidden at max region level and CastleLevel ActionSection is hidden at max castle level, with Build/Upgrade using `SelectTooltipModalUpkeep` for castle upkeep + build time, Promote using the upkeep tooltip from region level 2 onward, and other actions using message-only tooltips; garrison unit icon hover (Region defenders + Armies cards) reuses `SelectTooltipModalNoRes` to show localized unit names with fixed ActionTooltip X and garrison-row Y; actions are hard-disabled for the rest of the turn when `current_region.just_conquered_this_turn` is true (tooltip key: `conquered_region_blocked`); enemy/neutral regions use intel mode in non-debug view (hide action sections, keep resources visible, show last-known garrison/army composition, show army intel age in turns, and block enemy army selection), and returning to owned regions restores Raise Army/Garrison action sections; Armies-tab tutorial UI click events are sent only when tutorial mode is enabled; during scenario SpawnEvent placement mode it can be locked to Armies-only (Region tab + TAB switching disabled temporarily); emits `closed_info_modal` when the panel transitions from visible to hidden
│	├ RegionTooltip [scenes/region_tooltip.tscn] [region_tooltip.gd]
│	├ RegionTooltip2 [scenes/region_tooltip2.tscn] [region_tooltip_2.gd] – compact hover panel that now shows a combined Gold/Iron icon with `?` whenever the region can still discover ore (ore attempts left, eligible biome, nothing discovered yet); when `GameManager.debug_mode` is true or map editor mode is active, region title appends the region ID as `[#<id>]`
│	├ MoveTooltip [scenes/move_tooltip.tscn] [move_tooltip.gd] – hover cost badge shown only during army move target previews (MP text with 20px cursor offset)
│	├ PrebattleModal [scenes/prebattle_modal.tscn] [prebattle_modal.gd] – human-only step after moving into enemy territory; active UI bindings use the `Battle/VBoxContainer` branch (not legacy `Panel/Army`); supports siege equipment buying (spends wood immediately + siege points from defender count, and checks the current wood balance for each next purchase with no AI wood-budget cap), shows a Bombard step when trebuchets are purchased (4x50% shots per treb), allows Bombard without ladders/rams, and if post-bombard assault remains 0% then Attack stays disabled (withdraw-only); plays `SoundManager.play_catapult_sound()` when Bombard is used, posts the breach result in the message row, and combines ladder, wall-breach, and gate damage into an assault effectiveness sent to BattleManager; opening the modal now immediately hides both move-cost and region hover tooltips; `Space` triggers the current Attack button action (Attack/Bombard) when enabled; Withdraw refunds purchases and retreats via BattleManager; siege equipment rows use the Info hover panel like Recruitment (Ladders/Rams/Trebuchet), and Info body labels are localized via `prebattle_info_ladders`, `prebattle_info_siege_rams`, and `prebattle_info_trebuchet`; Siege panel shows per-section wall status and per-gate HP bars (ram targeting controls hidden by default); non-castle fights display 100% assault; when siege is available, `Battle/VBoxContainer/MarginContainer4` spacer is reduced to y=90 (otherwise y=118); ladders/rams no longer reduce castle defense pre-battle
│	├ BattleModal [scenes/battle_modal.tscn] [battle_modal.gd] – shares InfoModal standard/header themes for text styling; uses the `Battle` layout (Label/Label2 per unit row) to show attacker/defender counts, castle defense bonus, and player-colored attacker label; `ProgressBarArmy1/ProgressBarArmy2` track current combat power percent (unit `power` weighted) from 100% baseline and switch fill color by thirds (`67-100` green, `34-66` yellow, `0-33` red); defender baseline includes all starting defender sources (armies + garrison + recruits) so first-round losses update immediately; defender vigor label is computed once at battle start as a units-weighted average of defending armies' efficiency plus garrison at 100% (not recalculated mid-battle); attacker vigor display and battle simulation vigor now apply optional terrain penalties from `GameParameters` (forest/hills/forest_hills), and battle logs include a centered pre-combat difficult-terrain vigor reduction line when applicable; old legacy `Panel` subtree was removed, and root `BattleModal` no longer draws a custom frame/background; Quick Resolve button drops battle round time to the quick parameter for immediate simulation and can be triggered with `Space` while battle is in progress; when battle is over, configured `Continue/Close` key (default `Space`) or `Enter` triggers the same flow as Continue, but during tutorial only `Enter` hotkey is accepted; non-castle fights always show 100% assault; siege view updates gates/ram assignment each round with dynamic gate assault bonus and per-gate breach rolls (rams/ranged split, scheduled gate/ram damage over the breach duration); dual-human battles show both withdraw controls (`SecondPlayer/Withdraw` triggers Target/attacker side, main `Button` triggers Source/defender side), while non-dual-human keeps legacy single-button withdraw logic; siege panel hidden automatically for non-castle battles; `Stats/Body/LogsScroll/Logsdata` is now a scrollable rich-text battle log panel showing centered section headings (`Prebattle volleys`, `Battle starts`, `Player X withdraws`), centered turns, per-unit casualty breakdowns (attacker side first), and withdrawal/free-hit notes, and it auto-scrolls to the latest entry; `BattleLogs` button and stats/logs panel are hidden in tutorial mode only.
│	├ SiegePanel [scenes/siege.tscn] [siege_panel.gd] – shared siege status view used by Prebattle and Battle modals to show wall/gate damage and ram placement from the current siege payload (one active ram per gate; global reserve pool fills empty, intact gates)
│	├ BattleSummaryModal [scenes/battle_summary_modal.tscn] [battle_summary_modal.gd] – uses same InfoModal body/header themes; configured `Continue/Close` key (default `Space`) or `Enter` triggers the same close flow as Continue, but during tutorial only `Enter` hotkey is accepted; when summary shows `Battle Lost!` it plays `SoundManager.play_defeat_sound(3.0)` at 50% volume except in dual-human battles
│	├ TutorialModal [tutorial_modal.tscn] [tutorial_modal.gd] – UI-layer arrows for screen-anchored tutorial hints (Arrow1/Arrow2 placed manually in scene; all tutorial arrows use `images/icons/arrow_blue.png`)
│	├ PlayerStatusModal [scenes/player_status_modal.tscn] [player_status_modal.gd]
│	├ PlayerStatusModal2 [scenes/player_status_modal2.tscn] [player_status_modal2.gd] – resource HUD; hides RegionTooltip2 on mouse enter; shows projected next-turn income/pop growth from live state and refreshes via the `player_status_refresh_requested` signal (emitted by GameManager and actions like recruitment, ore search, promotions, castle construction, call to arms, battles, and raise-army); InfoModal region actions also force an immediate direct refresh after emission so income deltas update in the same click flow; visible at all times in `debug_mode`, otherwise only during human turns outside castle placement
│	├ TurnModal [scenes/turn_modal.tscn] [turn_modal.gd] – turn display; mouse enter hides RegionTooltip2; player label uses exact `GameParameters.get_player_color(current_player)` with 2px outline; during AI turns the end-turn button shows `in progress` and is disabled; `Enter` (or keypad Enter) triggers End Turn when the button is visible and enabled, and End Turn closes blocking modals (including battle/prebattle/summary) before advancing turn
│	├ RecruitmentModal [scenes/recruitment_modal.tscn] [recruitment_modal.gd] – per-unit cards with plus/minus count, per-unit Recruit button, and cost/attack/defense/abilities from GameParameters/UnitTraitEnum; plus/minus buttons use dark/light/disabled textures and disable correctly (`-` when count is 0, `+` when recruit/resources cap is reached); available recruits are pooled from owned neighbors and recruitment deducts proportionally; opening recruitment hides any region hover tooltip; successful `Recruit` and `Recruit All` button actions play `SoundManager.play_recruit_sound()`; total-cost row actions are wired through `TotalSection/HBoxContainer/Buttons` (`RecruitAll` left, `Close` right via spacer while the `Buttons` block fills remaining row width), and `Recruit All` is disabled when no units are staged or staged gold total is `0`; all unit cards now use the same `TextureRect`-based `AttackIcon` as Peasants; unit hover shows stat/trait panel; trait descriptions now use stable translation keys (`trait_desc_*`) resolved from trait IDs (not raw label text), so wording/typo changes no longer break localization; tutorial UI targets `RecruitmentModal/1archers` (first Archer added, 0->>=1), `RecruitmentModal/10archers` (staged Archer count reaches exactly 10), and `RecruitmentModal/recruit_all` (Recruit All press with staged units) are fired
│	├ CallToArmsModal [scenes/call_to_arms_modal.tscn] [call_to_arms_modal.gd] – transfer-modal layout; region rows use +/-10/1 buttons to shift recruits into target; buttons styled via themes/number_buttons_theme.tres and total row tracks remaining recruits; moved recruits now increase target population so later recruitment doesn’t overdraw population
│	├ TransferSoldiersModal [scenes/transfer_soldiers_modal.tscn] [transfer_soldiers_modal.gd] – transfer sliders/buttons move units between army/garrison; includes a scene-level mouse blocker so underlying InfoModal tabs/actions cannot be clicked while transfer is open; garrison can reach 0, and transferring all soldiers out of an army shows the embedded `Disband` confirmation before applying the transfer and removing the empty army; labels use standard modal text theme with white value columns like recruitment; bottom actions use `Transfer`/`Cancel` (Transfer starts disabled and enables only when any slider change differs from initial state; Cancel closes without applying changes or MP cost); human transfer MP rules: Army↔Army syncs the receiver to the lower MP before charging both armies `-1 MP`, Castle↔Army charges only the army `-1 MP`, and transfer MP never drops below 0
│	├ TransferSelectModal [scenes/transfer_select_modal.tscn] [transfer_select_modal.gd] – mirrors select modal layout; header + 40px white buttons for garrison/army targets; army targets with 0 MP are disabled and show tooltip `Army has no movement points`
│	├ SelectTooltipModal [scenes/select_tooltip_modal.tscn] [select_tooltip_modal.gd] – resource icon paths use existing textures (`new_food`, `new_forest`, `stone`, `new_iron`, `new_coin`); supports turns-only display mode (time icon + value only) for older action-tooltip flows
│	├ SelectTooltipModalNoRes [scenes/select_tooltip_modal_nores.tscn] [select_tooltip_modal_nores.gd] – message-only tooltip; ore search displays discovered-ore message with mine output when ore exists
│	├ SelectTooltipModalUpkeep [scenes/select_tooltip_modal_upkeep.tscn] [select_tooltip_modal_upkeep.gd] – InfoModal promote/build/upgrade upkeep tooltip; Region mode shows next-level food/wood/stone upkeep, Castle mode shows castle wood/stone upkeep plus build time, and zero-value upkeep resources hide their icon/value
│	├ IntroMessageModal [scenes/intromessage_modal.tscn] [intro_message_modal.gd] – intro/custom-map message overlay; demo mode keeps the localized `skirmish-intro` body and appends the selected custom-map victory-condition description (`conquer_condition_description` / `dominate_condition_description`), while normal custom-map/skirmish starts show the selected map display name, localized `Objective` heading, and current victory objective; uses `Place Castle` in custom-map flow, uses `Continue` for scenario intro text and objectives-icon reopen, and supports campaign outro display with `End Mission` button text after active gameplay modals are closed; long text is shown in a scrollable message area; intro/game-guide text styling is shared through `themes/message_modals_theme.tres`; intro scroll body uses `RichTextLabel` (same themed font pipeline as GameGuideModal), with scene-defined layout and standard ScrollContainer overflow behavior.
│	├ GameGuideModal [scenes/game_guide_modal.tscn] [game_guide_modal.gd] – gameplay guide modal using `RichTextLabel` + scroll; loads section BBCode files from `translations/game_guide/sections/game_guide.{en,de,pl,br}.{regions,economy,recruitment,battles,tips}.bbcode` via locale fallback to `en`; `Continue` closes guide and returns to `GameGuideMenuModal`; uses shared `themes/message_modals_theme.tres`
│	├ EventMessageModal [scenes/event_message_modal.tscn] [event_message_modal.gd] – dedicated scenario-event message overlay using `MessageModal` behavior with a 2x larger centered message box texture, expanded text area, and unchanged bottom `Continue` button placement
│	├ MessageModal [scenes/message_modal.tscn] [message_modal.gd] – tutorial/general message overlay; calls `move_to_front()` when shown so Continue can be clicked above InfoModal; configured `Continue/Close` key (default `Space`), `Enter`, and keypad `Enter` trigger the same close flow as Continue, while `Esc` also closes in non-tutorial mode; key input is marked handled before Continue signals run so terminal outcome handlers can change scenes safely; in tutorial mode, configured `Continue/Close` key and `Esc` are ignored by MessageModal so GameMenu routing handles it; `MessageLabel` font auto-scales by text length (`>155` chars: `-1px`, `>160` chars: `-2px`) while button font stays unchanged
│	├ NextPlayerModal [scenes/next_player_modal.tscn] [next_player_modal.gd] – shows turn prompt with `HeaderLabel` as `Turn <n>`; shown on every human turn (never for AI) outside castle placement; with 2+ humans it blocks turn start until `Continue`, while single-human turns use auto-hide; player label now uses exact `GameParameters.get_player_color(player_id)` to match `TurnModal`.
│	├ MoveModal [scenes/move_modal.tscn] [move_modal.gd] – cancel move UI; hides region tooltip when hovering any MoveModal control; gameplay action buttons (Recruit/Make Camp/Transfer) are disabled and ignored when the selected army has 0 MP; hovering those disabled actions shows `NoActions` with text `Not enough movement points`; `Next Army` remains enabled as a UI cycling action; make-camp refreshes move target highlights after MP changes and refreshes InfoModal when open; ArmyActions button cancels move and opens InfoModal for the selected army; emits tutorial action `MoveModal/army_deselect` when ArmyManager deselects the active army; reopening MoveModal now always refreshes selected-army move targets so move-cost overlays stay in sync after recruitment/composition changes; `show_move_modal()`/`hide_move_modal()` resolve `UIManager` directly so overlay suppression also works during early load-time deselection; in tutorial mode, MoveModal ignores `Esc` so cancel/deselect is not triggered by MoveModal hotkey handling; NextArmy button still cycles armies; keyboard shortcuts for MoveModal actions are disabled during tutorial to preserve tutorial step flow, and outside tutorial the NextArmy shortcut cycles to the player's next army unless recruitment/transfer modals are open, repainting modals and centering the camera
│	├ MoveModal [scenes/move_modal.tscn] [move_modal.gd] – cancel move UI; hides region tooltip when hovering any MoveModal control; gameplay action buttons (Recruit/Make Camp/Transfer) are disabled and ignored when the selected army has 0 MP; hovering those disabled actions shows `NoActions` with text `Not enough movement points`; `Next Army` remains enabled as a UI cycling action; make-camp refreshes move target highlights after MP changes and refreshes InfoModal when open; ArmyActions button cancels move and opens InfoModal for the selected army; emits tutorial action `MoveModal/army_deselect` when ArmyManager deselects the active army; reopening MoveModal now always refreshes selected-army move targets so move-cost overlays stay in sync after recruitment/composition changes; `show_move_modal()`/`hide_move_modal()` resolve `UIManager` directly so overlay suppression also works during early load-time deselection; `NoActions` tooltip node is resolved with direct `get_node("NoActions")` in show/hide helpers so calls are safe in early modal flow before `_ready`; NextArmy button still cycles armies; keyboard shortcuts for MoveModal actions are disabled during tutorial to preserve tutorial step flow, and outside tutorial the NextArmy shortcut cycles to the player's next army unless recruitment/transfer modals are open, repainting modals and centering the camera
│	├ IconsModal [scenes/icons_modal.tscn] [icons_modal.gd] – swaps scale/settings/objectives icon textures on hover; on icon hover hides RegionTooltip2 and clears current region hover highlights; scale icon opens TradeModal, settings icon opens GameMenuModal, objectives icon reopens IntroMessageModal with `Continue` button text
│	├ SpeedModal [scenes/speed_modal.tscn] [scenes/speed_modal.gd] – AI/battle speed selector (normal/fast/very fast); adjusts AI move speed multiplier during map play and battle round timing during sieges; selected option stays visibly pressed via grouped toggle buttons; hides region tooltip on hover; also shown during battles
│	├ TradeModal [scenes/trade_modal.tscn] [trade_modal.gd] – new Trade layout; plus/minus adjust staged buy/sell counts and gold balance (dark/light/disabled textures), `-` disables at 0 count and `+` disables when no buy/sell is possible for that side, sell adjustments reset staged buy for that resource, Buy/Sell button clicks play `SoundManager.play_trade_sound()`, successful trades apply immediately via TradeManager and refresh InfoModal when visible, and Buy/Sell buttons are non-toggle so highlight follows hover only; Close just hides the modal. When scenario `trade_disabled=true`, unit trade controls are hidden, `ButtonEnabled` is hidden, `ButtonDisabled` is shown, and `Trade is disabled in this scenario` is shown.
│	└ MapEditorPanel [scenes/map_editor_panel.tscn] [map_editor_panel.gd]
└ Players (Node2D)
	├ Player1..Player6 (Node2D) – reserved containers

Notes
- UI modals are extracted into separate `.tscn` scenes and instanced under `UI` (no longer inline).
- Select modals (general/army/region) are not instanced in `main.tscn`; owned-region clicks use InfoModal instead.
- InfoModal region actions are ownership-gated (`owner == current turn player`) for both UI and execution: enemy/neutral regions hide action sections/buttons and handlers reject raise/recruit/build/promote/ore actions; Raise Army shows a `Needs Keep` requirement instead of button/cost until the region has at least a Keep.
- Regions, armies, castles are created at runtime under `Map/Regions`.
- Map background tiles `sea_new.png` (Sprite2D region repeat) without stretching; ocean visuals rely on base layer only; land polygons rebuild from border noise to stay aligned (mountain clusters rely on hidden noisy borders for seamless joins).
- Ownership borders render as solid Line2D strokes.

Map Editor Panel (editor mode)
- Uses TabContainer with 5 tabs: Main, Scenario, Region, Army, Event
  - Main Tab: Save/Load scenario functionality (scenario name, type, mission number, `Set for Difficulty` selector, custom-map `display_name_key`, save buttons; intro/description/objectives text fields were removed)
  - Save Scenario: Input + button. Writes JSON to `res://scenarios/<scenario_name>.json` (or `last_saved_scenario.json` if empty) with:
    - `map_file` (source mapdata JSON path)
    - `intro_message`, `description`, and `objectives` set as translation keys using scenario name suffixes (`<scenario_name>_intro`, `<scenario_name>_description`, `<scenario_name>_objectives`)
    - `skip_intro` preserved from existing scenario file when overwriting (defaults to `false` for new files)
    - On save, missing translation rows for those three keys are appended to `translations/hotw_translations.csv` (all locales initialized with the key string)
    - `scenario_type` (`scenario` or `campaign`, defaults to `scenario` when missing) and `mission_number` (saved for campaign type from Main tab mission selector)
    - `regions`: list with id, name, biome/ocean/type_display, level, castle_type, population, owner, optional score_bonus, resources, discovered_ores (ocean and mountain regions are not saved; resources are saved as base values without region-level multiplier baked in); baseline `garrison` is saved only for regions explicitly customized in editor.
    - `armies`: list with region_id, player_id, name, composition (per unit type)
    - `player_settings`: six entries (player_id + control_type: Player/Computer/Off)
    - `player_resources`: six entries (player_id + resource dictionary for Gold/Wood/Food/Stone/Iron). Applied to `PlayerManagerNode` by `ScenarioManager.apply_to_runtime()` when the scenario starts.
    - optional `difficulty_overrides` blocks (`easy|normal|hard`) for difficulty-specific `player_resources`, `army_compositions`, `garrison_compositions`, and `event_compositions` (`event_index + composition`)
    - `difficulty`: scenario difficulty availability (`all|easy|normal|hard`), edited in Scenario tab difficulty dropdown (`all` means main menu allows player selection)
  - Load Scenario: Lists all `res://scenarios/*.json` in a select box with "Load" button (no-op for now)
  - Save Map: Persists edited base map biomes, ocean flags, and optional top-level `display_name_key` back to the selected `res://mapdata/<file>.json`; saving a non-empty display key appends a placeholder translation row to `translations/hotw_translations.csv`, while clearing it removes the key from the map JSON.
  - `Set for Difficulty` editor scope:
    - `all`: edits baseline values (`player_resources`, `armies[].composition`, `regions[].garrison`)
    - `easy|normal|hard`: edits override values only; army add/remove remains available only in `all`; Event tab saves army composition overrides only (event metadata stays baseline)
- Scenario Tab: Player control toggles plus starting resource editor (dropdown for Player 1–6, rows for Gold/Wood/Food/Stone/Iron with editable amounts saved to `player_resources`), `Disable Trade` checkbox (saved as scenario `trade_disabled`), `Disable Neutral Cluster Bonus` checkbox (saved as scenario `disable_neutral_cluster_bonus`), difficulty dropdown (`All|Easy|Normal|Hard`, saved as scenario `difficulty`), and a victory-condition editor with 6 types (`Conquer`, `Conquer After Events`, `Dominate`, `Own Region`, `Survive Turns`, `Economy`). Condition-specific fields appear by selection (Own Region: target player + region id; Survive Turns: target player + turns + region id; Economy: target player + optional region id/region level/castle level/unit target plus optional resource type/amount target) and save into scenario `victory_conditions`.
- Event Tab: scenario-only event editor with Add/Event list (name + Edit/Delete) and in-panel editor view (Back, event name, comma-separated region IDs, turn range start/end, player select, army composition, custom message, Save). Saved into scenario `events`.
- Region Tab: ID, Name, Type, Level, Population, Score Bonus, Castle, Resources, Ore, Ownership
  - Resource values in editor are base resource values (pre region-level multiplier), and edits write base values.
  - Score Bonus saves optional scenario-only per-region `score_bonus` (omitted when 0) for AI attack target scoring only; `ArmyTargetScorer.score_region_base()` adds it directly to the region's 0-100 attack base score.
  - Ownership: OptionButton with Neutral (0) and Players 1–6. Emits `region_type_changed` with `OWNER:<id>`
  - Ore editor: per-region guaranteed ore discovery attempt for scenarios (`ore_guaranteed_discovery_attempt`) saved in scenario region data; attempt is a select option (`0`, `1..ORE_SEARCH_CHANCES_PER_REGION`, `random`), where `0` is normal non-guaranteed behavior and ore type is derived from the region Gold/Iron values.
- Army Tab: Army management with two states
  - Default state: Army toggle button (Add Army/Remove Army when owner > 0; hidden for Neutral), Edit Army button
  - Edit state: Shows Region ID/Name, Army Name, and per-unit counts (editable). "Close" saves changes and returns to default
- MapEditor handles signals by delegating to `RegionManager.set_region_ownership` (creates/removes overlays, regenerates borders) and `ArmyManager.create_raised_army` / removal. Region resources are generated by `RegionManager` after the map is generated in editor mode.
- Region type changes in editor now reinitialize region runtime state immediately (population, recruits, ore-search state, and resources): ocean resets to zero-state; land regenerates population/resources by current level and biome.
 - Quick ownership mode: Press number keys `1..6` in editor to enable ownership mode for that player; next region click assigns ownership. Press `ESC` to exit ownership mode.
- When editing army, Army tab stays active with edit panel visible until closed

editor_start.tscn (Editor Start)
- Start UI when `enable_map_editor = true`.
- Map Data: lists `mapdata-*.json` under `res://mapdata/`; “Edit Map or New Scenario” passes `res://mapdata/<filename>` to `MapGenerator.data_file_path`, then loads `main.tscn` and applies compatibility `map_size` from map profile resolution.
- Map Data: lists `mapdata-*.json` under `res://mapdata/`; code always reads map files from this folder. Named map JSONs use top-level `display_name_key` and `non_ocean_region_count` (maintained by `python/update_named_map_region_counts.py`). Frontend size display/filtering is based on non-ocean regions: XS `1-100`, S `101-200`, M `201-350`, L `351+`; physical map scale/zoom still uses total JSON region count. EditorStart scans scenarios and appends referencing scenario filenames in brackets to map rows. Named maps show first, sorted by frontend size order `XS/S/M/L`, using `<frontend size code> <display name> <preview marker> [scenario.json]` where display name comes from translated top-level map JSON `display_name_key` and preview marker is `+` when `res://previews/<map base>.png` exists, otherwise `-`; unnamed maps remain below as plain filenames with the same optional scenario bracket in alphabetical order. The real filename is stored as item metadata for loading. `MapGenerator.data_file_path` expects a bare filename (no `res://` or folder). Import token parsing supports both legacy hyphen and underscore naming; last token can be a textual size (`tiny/small/medium/large/huge`, plus legacy `xtiny/xxtiny`) or a numeric region count.
- Scenario: lists `res://scenarios/*.json`; loader always resolves scenarios from `res://scenarios/` and accepts bare filenames or full paths. Scenario `map_file` field should be a bare filename; loader resolves from `res://mapdata/`.
- Scenario: during load, EditorStart resolves scenario map compatibility `map_size` from map profile (`filename token + map JSON regions`) and applies `_map_set_size_from_string()` before generating the map.
- Launch: EditorStart sets `SceneTree` meta `editor_start_payload` and switches to `main.tscn`. `GameManager._ready()` force-enables editor mode when this payload is present and calls `_initialize_map_editor()` to show `MapEditorPanel`.
- EditorStart payloads now tag the session as `game_mode="custom"` for raw map editing (clearing any previous scenario names) or `game_mode="scenario"` for scenario editing, so screenshots/previews automatically save under the selected map data or scenario name.
- MapEditorPanel Main tab includes Delete Map; it shows a confirmation popup, removes the loaded map JSON file from `res://mapdata/`, and returns to EditorStart after successful deletion.

scenes/voronoi_map_generator.tscn (VoronoiMapGenerator)
- Editor tool scene that previews a random Voronoi layout without running the game. Exported slider params: `seed`, `random_seed`, `size`, `noise_frequency`, `noise_octaves`, `noise_gain`, `water_level`, `edge_clear_level`, `elevation_profile`, `hill_split`, and `save_map` (trigger save); changing any triggers auto-regeneration. Save writes `res://mapdata/mapdata-<seed>-medium.json` compatible with `map_generator.gd`.
- Builds color-coded terrain (green flats, brown hills, gray mountains, blue water) inside a 1024x1024 bounds with red site markers; per-site noise tags regions as water vs land, elevation slider shifts mountain heights, hill split controls grass vs hill coverage, and optional edge clearing forces edge-touching regions to water.

### scenes/warrior_1.tscn .. scenes/warrior_6.tscn
- Animated army icon scenes (Node2D root with `Animations`/`Victory` AnimatedSprite2D nodes) used for map armies; Army starts the `Animations` idle when instancing (warrior_1 also plays idle via `animation.gd`).

### scenes/upgrade_card.tscn
- Reusable upgrade-card UI template used by the main-menu `Cards` grid and `CardModal`. Root uses `upgrade_card.gd` exported scene properties for card texture, title key, icon texture/placement, and description key, with card typography centralized in `themes/card_font_theme.tres`; child names (`CardTexture`, `CardTitle`, `CardIcon`, `CardText`, `Level1..3`, `TextureRect6`, `LevelText`, `Lock`) are stable so `main_menu.gd` can refresh unlock state and selection textures.

### scenes/card_modal.tscn
- 460x690 centered upgrade-card popup using `card_modal.gd`; hides other main-scene UI modal/tooltip controls before showing, then displays `Card Unlocked` or `Card Level Up`, a centered 248x330 `UpgradeCard` instance, level icons, and a `Continue` button.

## Core Managers & Flow

### upgrades_manager.gd (UpgradesManager)
- Purpose: Global upgrade-card catalog, per-card level bonus table, encrypted progress persistence (`user://upgrade_progress.cfg` levels + source `completions` + `stats.total_games`, with one-time plaintext migration), unlocked-card availability checks, source-completion checks for campaign progression, card selection validation, victory unlock level recording, and resource/unit bonus lookup.
- Unlock mapping: Card1 Food level 1 from tutorial, levels 2/3 from 5/10 qualified wins; Card2 Wood from mission-2 Easy/Normal/Hard; Card3 Stone from Vikings Invasion Easy/Normal/Hard; Card4 Iron from Last Stand Easy/Normal/Hard; Card5 Gold from horde Easy/Normal/Hard; Card6 Peasants from mission-1 completion, then Forgotten Kingdom/scenario4 Easy/Normal; Card7-11 from mission-3, mission-4, mission-5, mission-6, mission7 Easy/Normal/Hard; Card12 Knights from skirmish Easy/Normal/Hard. Qualified wins are human victories in scenarios/campaign missions, plus custom-map/skirmish wins only when at least one computer opponent is active. `record_completion_result(...)` returns the first changed card id plus previous/new level for victory card popups.

### game_manager.gd (GameManager)
- Purpose: High-level orchestration (castle placement, turns, resource ticks, AI start, battle finalization).
- Key: `initialize_managers()`, `next_turn()`, `_process_round_start_actions()`, `_process_player_turn_start()`, `handle_castle_placement()`, `finalize_battle_result(...)`.
- Integrates: RegionManager, ArmyManager, BattleManager, VisualManager, UIManager, PlayerManagerNode, TurnController, AIDebugVisualizer, CastlePlacementScorer.
- Temporary debug: F3 converts Player 1 from Human to Computer at runtime and refreshes current-player UI when Player 1 is active.
- Human End Turn: deselects the active army before auto-camp/turn advancement so MoveModal, move arrows, and move-region highlights close through the normal ArmyManager deselect path.
- Victory system: stores normalized `victory_conditions`; checks at turn start and after movement/battle; supports `conquer` (others have no castles and no armies), `conquer_after_events` (same conquer check, but only after all runtime scenario events are marked triggered), `dominate` (>=70% conquerable regions: non-ocean, non-mountain), scenario-targeted `own_region` (human target wins by owning the region; scenario fails if another player conquers that target region first), `survive_turns` (human target wins at required turn; optional `region_id` requires that human to keep holding the region before then, otherwise scenario fails; legacy no-region conditions still use castle/army survival failure), and `economy` (human-only; `region_id > 0` requires owning/upgrading that region, `resource_amount > 0` requires holding that amount of `resource_type`, and `units_hired > 0` requires globally hired units of `unit_type` regardless of army/garrison/region destination; `region_id = 0` skips region checks). Empty list means no automatic win trigger.
- Economy tracking: `record_hired_units()` tracks per-player hired totals by unit type (shared across human and AI recruitment flows), immediately rechecks human victory conditions after new hires, and persists totals in saves as `player_hired_units`.
- Analytics tracking: starts content runs for scenario/campaign/custom-map starts, classifies campaign scenarios as `mission`, standalone scenarios as `scenario`, custom maps as `skirmish`, heartbeats active runs with elapsed seconds/current turn, finishes runs as `won`, terminal `lost`, or `quit`, and persists active run data in saves as `game_state.analytics_run`.
- Scenario rules: `scenario_trade_disabled` is loaded from scenario JSON `trade_disabled` and exposed via `is_trade_disabled_for_current_game()` for TradeModal rendering; `scenario_disable_neutral_cluster_bonus` is loaded from `disable_neutral_cluster_bonus` and exposed via `is_neutral_cluster_bonus_disabled_for_current_game()` for AI neutral-cluster scoring.
- Win/fail flow: outcome resolution is victory-first, but scenario/campaign AI victory is displayed as mission failure (`You failed`) and exits like terminal human loss; human victory still shows `Player X Won` or the campaign outro. Human elimination is evaluated after victory checks; scenario humans with no castle/army are not eliminated while they still have an untriggered event scheduled for the current or a future turn. Eliminated humans are marked `OFF` (removed from turn order), show `Player X lost`, and play continues while at least one human remains. If no humans remain, Continue exits to main menu; for campaign scenarios it routes to `main_menu_target = "campaign_list"`. Single-human terminal defeat uses `You failed`. For regular victory popups (`Player X Won`), Continue returns to `main_menu.tscn`; campaign Player 1 uses the dedicated outro path to campaign list. If victory unlocks or levels an upgrade card, the victory/outro Continue shows `CardModal` first, and its Continue proceeds to the same main-menu target.
- Empty scenario human turns: after current-turn events, turn-start processing, and victory/elimination checks, a scenario human turn auto-advances if that player has no owned regions, no castles, no armies, and no remaining untriggered event for the current turn. This skips placeholder turns before later spawn events without affecting castle-placement/skirmish flow.
- Scenario events: on scenario init, each event pre-rolls a single `turn` from `[turn_start, turn_end]` and shuffles candidate region IDs once. At matching player turn start (before normal turn-start economy/actions), AI/off-player events keep legacy auto-spawn behavior (first non-full valid region from shuffled pool, then immediate battle check). Human-owned events now use manual SpawnEvent placement: event message (shown in `EventMessageModal`) -> armies-only InfoModal lock with spawned army selected -> highlighted allowed target regions with custom arrows from adjacent ocean sources -> right-click target to place -> immediate battle check -> spawned army movement points forced to 0 after placement resolution. Human event messages focus the camera on the event’s first configured region ID; AI events focus on the actual auto-selected spawn region.
- Scenario intro: `IntroMessageModal` is shown before the first turn starts using scenario `intro_message` plus `objectives` (joined with one blank line when both exist, each resolved via `tr(...)`). Optional scenario JSON key `skip_intro` (default `false`) skips this auto-show at scenario start. Campaign victory outro text uses key `mission-<mission_number>_outro`. `objectives` is persisted in scenario JSON and shown on main-menu scenario panel. Editor saves intro/description/objectives fields as translation keys.
- Debug: `debug_disable_battle_modal` — when true, AI battles run instantly in background (no modal, no delays).
- `debug_mode`: when false, AI turns hide the PlayerStatus panel, IconsModal (trade/map icons), and TurnModal end-turn button (keeps AI HUD minimal).
- Upgrade cards and campaign missions: when `debug_mode` is true, all upgrade cards are treated as unlocked at level 3 for pre-game selection and bonus application, and all campaign missions are selectable, without writing fake unlocks into upgrade progress.
- Debug language shortcut: when `debug_mode` is true, pressing `Z` cycles locales in-game (`en` → `de` → `pl` → `br`), refreshes `TurnModal` text immediately, and saves the selected locale to settings.
- Debug instant win shortcut: when `debug_mode` is true, pressing `F1` declares a normal victory for the current human player, or the first active human if the current turn is AI, so card unlocks and campaign outro/card-modal flows can be tested quickly.
- Camera: Sets initial zoom from map profile (`region_count`-anchored interpolation preserving known values: Small=1.0, Medium=1.5, Large=2.0) and clamps zoom-out to 0.5.
- AI memory: Records observed enemy army power for computer players after each battle (5-round decay window via `GameParameters.ENEMY_ARMY_MEMORY_ROUNDS`).
- AI memory: Records observed enemy garrison power when computer armies attack a region (shared 5-round decay window).
- Enemy intel snapshots: `_record_both_sides_power_snapshot()` captures defender forces for the attacker and attacker forces for the defender on early withdrawals; `_record_battle_power_after_resolution()` refreshes both sides' sightings after losses/retreats so AI knowledge stays current.
- Siege sims: `simulate_siege_battle(attacker, region)` runs AI-style siege prep on cloned wall/gate states (no resource spend/state changes) and feeds `BattleSimulator` so AI can judge castle fights during planning; it uses known defender intel (tracked armies/garrison) for attacker-side scouting.
- Castle threat sims: `simulate_castle_threat_battle(attacking_armies, region)` merges multiple attackers from one nearby region into a single simulated composition and runs against full defenders (all defending armies + garrison + recruits) for castle-threat classification.
- AI: Uses `TurnController.start_turn()` for computer players; castle placement uses `CastlePlacementScorer` with randomness.
- AI turn handoff: after an AI turn completes, `next_turn()` is queued via `call_deferred` (not immediate recursion), and turn advancement always waits for pending battles plus battle finalization (`_await_pending_battles()` + `BattleManager.await_finalize_complete()`) before switching players.
- AI turn safety: TurnController now skips invalid/freed army handles before per-army processing and guards decision-cycle continuation checks against freed references, preventing async turn-loop crashes when an army is removed mid-cycle.
- AI battle edge safety: threat-attack turn locks now use pre-captured army instance IDs across awaited battle movement, and GameManager AI battle-log queue/token helpers ignore freed army references, preventing `get_instance_id()` calls on destroyed armies.
- Turn-advance guard: `next_turn()` now uses an internal in-progress lock to prevent overlapping/re-entrant turn transitions (e.g. duplicate end-turn triggers) from skipping players.
- AI Step Gate: `ai_step_requires_shift` forwards to `DebugStepGate` — when true, AI waits for Shift to advance; false lets AI run automatically.
- AI Logging: Owns `AILogManager`; `ensure_ai_log_started()` names the log `<scenario>_<timestamp>.txt` the first time any AI player takes a turn so all later TurnController events can append to the same file inside the root `logs/` directory; file logging is enabled only when `debug_mode` is true.
- Round start caches the average power of all armies each round so AI economy can compare castle garrisons against the global baseline.
- AI Siege: `_execute_ai_siege_preparation()` rolls trebuchets between `(castle_level - 1 - destroyed_walls)` and intact wall sections, reduces by attacker/defender power advantage above 3:1 (defender power = garrison + recruits + owned armies), then clamps by siege points/wood/caps; rams spend random remaining points up to intact gates (respecting caps/wood), ladders take the leftover points. AI fires trebs once before battle (`trebuchet_bombard` payload, no double wall damage) and ships `siege_view_state` with ram HP.
- Castle assault gating: AI castle attacks now run a no-side-effect siege simulation before spending resources; only predicted victories proceed. Simulation logs include siege purchases, assault/defense values, and pre/post compositions.
- Battle Log Queue: `finalize_battle_result()` snapshots attacker/defender forces, applies losses from that queued battle snapshot, converts the result into readable lines (`Battle started`, attacker/defender power/composition, `Battle Result`, post-battle status), and queues them per attacking army + battle region so TurnController appends details under the matching movement `Fight at ...` log.
- Initial castle placement (human or AI) now spawns a Keep immediately rather than an Outpost, so every starting stronghold begins at level 2 with the associated recruitment caps and visuals.
- Debug overlay: `show_region_center_markers` draws a small red square at each land region center by calling `MapGenerator.set_center_markers_enabled()` after startup and whenever the map regenerates.
- AI pre-battle withdrawal: before siege prep, AI attackers back out if attacker power ≤ defender power after defense bonus; after siege prep (or for open-field fights) AI withdraws immediately when the standard withdrawal power threshold is met (no roll, no casualties).
- Starting army composition lookup now uses `game_difficulty`: human starts are difficulty-dependent (Easy > Normal > Hard), while AI starts stay unchanged.
- Starting player resources now use `game_difficulty` presets for active players at initialization (Easy: 130/150/30/10/20, Normal: 100/70/20/0/10, Hard: 50/50/10/0/0 for Gold/Food/Wood/Iron/Stone).
- Target scoring: known enemy power (tracked armies + tracked garrison) is checked against the same withdrawal thresholds; regions with stronger known enemies are skipped during frontier target selection.
- Target scoring: known enemy power is adjusted by castle defense and a conservative assault multiplier; castles require a projected ratio > 1.5 to be considered, while open-field targets require > 1.0; pursuit bonuses apply the same castle adjustments.
- Battle intel snapshots now record attacker army intel for the defending player regardless of defender control type, and defender intel is also captured at battle start so human defenders consistently keep enemy army intel for InfoModal.
- Map Editor: `enable_map_editor` flag — when true, skips castle placement/turns, initializes MapEditor, shows empty editor panel (25% left).
 - Modes:
   - Custom: current behavior (castle placement → gameplay).
- Scenario: set `game_mode="scenario"` and `scenario_path` before start; map is regenerated from scenario `map_file`, managers init, then `ScenarioManager.apply_to_runtime(..., game_difficulty)` is called. Skips castle placement and starts the first turn immediately.
- Demo-mode start guard: when `GameParameters.DEMO_MODE_ENABLED` is true, `GameManager` sanitizes `start_payload` targets so only whitelisted scenario files and custom-map files can be launched (`DEMO_ALLOWED_SCENARIO_FILES`, `DEMO_ALLOWED_CUSTOM_MAP_FILES`).
- Scenario player settings: when scenario JSON includes `player_settings`, `GameManager` applies them before runtime scenario data, reinitializes `PlayerManagerNode` with those types, and then resolves first turn from active players only.
- Scenario resources: `ScenarioManager.apply_to_runtime()` pushes `player_resources` into `PlayerManagerNode.set_player_resources()` before the first turn.
- Scenario events: `_initialize_scenario_events_from_data(...)` now resolves optional `difficulty_overrides.<difficulty>.event_compositions` before event turn/region randomization and spawn.
- Scenario event auto-spawn flow now spawns the event army before camera focus, then when immediate battle is required it waits a fixed real-time `1.0s` after camera focus before triggering battle so players can see where the spawn occurred.
- AI target-score overlay visibility is now strictly gated by `GameManager.debug_mode`; AI turns no longer auto-show `_ai_debug_visualizer` when debug mode is disabled.
- First turn: `_process_player_turn_start()` skips economy/food for each player the first time it runs so HUD deltas stay `+0`; actual income/upkeep begins when that player starts turn two.
- First turn startup now explicitly calls `SoundManager.play_game_music()` in `_start_first_turn()` so gameplay playlist playback begins reliably after menu/intro flows.
- Turn order: castle placement completion, scenario immediate start, and `next_turn()` round-boundary checks now anchor on the first active player in `players_per_round`, so `OFF` players are skipped consistently.
- Autosave: at normal human turn start (after turn-start processing/events and before waiting for player actions), `SaveGameManager.save_auto_save()` overwrites a single file `user://autosave.json`; autosave is disabled when `GameManager.tutorial_enabled` is true.
- Turn-start upkeep: after resource income and before army food upkeep/famine, GameManager deducts per-player region/castle resource upkeep (food/wood/stone) and clamps those resources to `0` when upkeep exceeds current balance.
- Famine: `_process_army_food_costs_for_player()` triggers `famine_regions()` when food falls short. For each 1.0 missing food, famine now rolls lookup points `1..10` with weighted odds from `[5, 7, 9, 13, 16, 16, 13, 9, 7, 5]` (middle values most likely), then computes base loss points as `floor(food_deficit * rolled_famine_points)`. After that, losses are applied proportionally across active armies and active garrisons; unit removal remains food-cost-aware (`0.1 food units = 1 point`, horsemen/mounted knights `0.2 = 2 points`). Population/growth are not modified by famine. `GameManager` stores per-player famine summaries (`triggered`, `food_deficit`, `target_points`, `actual_points_removed`, `soldiers_lost`, `by_force`, `by_unit_type`) via `get_latest_famine_result_for_player()` / `consume_latest_famine_result_for_player()`. On human turns, famine opens `MessageModal` at turn start only when `soldiers_lost > 0`, then continues after acknowledgement.

### scenario_manager.gd (ScenarioManager)
- Purpose: Data-driven loader for premade scenarios (map + region overrides + ownership + castles + armies).
- API: `load_scenario(path) -> Dictionary`, `apply_to_runtime(map_generator, region_manager, army_manager, visual_manager, scenario, player_manager, difficulty_token="all")`.
- Order of application: regions (type/ocean, level, population, resources, ores, name) → ownership (via RegionManager) → castles (via VisualManager) → armies (via ArmyManager).
- Region biome/ocean overrides now also sync into `MapGenerator.region_by_id` + `MapGenerator.regions`; after scenario deltas, affected region visuals are refreshed, MapGenerator non-ocean adjacency/nearby-region caches are rebuilt, and RegionManager rebuilds its pathing graph so scenario-specific terrain changes render and path correctly in editor and gameplay.
- Difficulty resolution: optional `difficulty_overrides.easy|normal|hard` can override categories `player_resources`, `army_compositions` (`region_id + composition`), and `garrison_compositions` (`region_id + composition`); each category falls back to baseline data when missing.
- Garrison load rule: for scenario-owned regions (`owner > 0`) without explicit baseline/difficulty garrison data, ScenarioManager clears generated default garrison so only editor-authored garrisons persist on owned starts.
- Scenario ore rules: enables region-level constrained ore discovery (`ore_scenario_rules_enabled=true`) so ore results respect scenario resource potentials; after terrain overrides, ore-capable region types keep search attempts while no ore is discovered, even when scenario Gold/Iron potential is 0; optional `ore_guaranteed_discovery_attempt` enforces guaranteed discovery on an exact attempt using the region Gold/Iron values, while `"random"` rolls a guaranteed attempt from `1..ORE_SEARCH_CHANCES_PER_REGION` at gameplay load and remains `random` in editor preview/save.
- Ownership setup treats scenario-owned regions as fully established: `set_initial_region_ownership()` is used for player IDs > 0 so they start turn one with full recruit availability.
- Region setup now refills recruit pools to the current max after applying population and castle overrides so scenario starts never begin partially depleted.
- Keeps base map geometry in the referenced `map_file`; scenario stores only deltas.

### save_game_manager.gd (SaveGameManager)
- Purpose: Runtime persistence with named save files in `user://` (`*.json`, default `user://savegame.json`); loading accepts encrypted and plaintext saves, while saving writes plaintext when `GameManager.debug_mode` is true and encrypted otherwise; global settings remain plaintext in `user://settings.cfg`.
- Autosave: dedicated `user://autosave.json` path, overwritten via `save_auto_save(game_manager)` at turn start in non-tutorial gameplay using the same debug/plaintext vs release/encrypted save format rule.
- Save payload: source descriptor (`mode`, `scenario_path`, `map_file`, compatibility `map_size`, player settings, `victory_conditions`, `trade_disabled`, `disable_neutral_cluster_bonus`) + game state (turn/current player/player types/castle placement flags, `scenario_events_runtime`, `player_hired_units`) + player manager snapshot + full mutable region/army snapshots (includes ore discovery state and scenario ore guarantee fields per region).
- Load flow: map is rebuilt from saved source first, then SaveGameManager reapplies runtime state (regions/ownership/castles/armies/player state and current turn pointers), reapplies player resource dictionaries explicitly, forces an immediate `PlayerStatusModal2` refresh, and resumes gameplay music through `SoundManager.play_game_music()`.
- Settings: persists sound/music enabled + dB values, clouds toggle, AI speed, battle speed, army move trigger, and battle log panel visibility (`gameplay.battle_logs_visible`); loaded in both main menu and in-game startup.

### TurnController.gd (TurnController)
- Purpose: Unified turn pipeline for AI (and fits human pipeline shape).
- Flow: frontier discovery → target scoring → build candidates → execute with step-gate → handle battle → continue/recalc.
- Victory integration: aborts AI turn/move loops when `GameManager` reports victory and runs `check_victory_conditions_for_player()` after each completed AI move action.
- Movement: Each AI army picks the best single frontier target to attack; if no valid target exists or merge policy halts on a stronger/unknown enemy, it logs defense and spends the turn camping.
- Frontier fallback: when no hard targets are reachable this turn, `MAIN` armies now advance toward the best unreachable hard target (highest score, then lower mp_cost) instead of camping.
- Neutral expansion scoring: neutral, non-castle, no-enemy-army targets add configurable `neutral_core_bonus` in final move scoring, based on distance 1..3 to the nearest owned castle plus one capped owned-neighbor bonus; they also add capped `neutral_cluster_bonus` for connected neutral soft-region clusters so large easy expansion pockets can compete with nearby enemy targets unless scenario `disable_neutral_cluster_bonus=true`, plus capped `neutral_border_bonus` per owned neighbor to favor compact expansion over thin lines; candidate logs include `random_modifier`, `neutral_core_bonus`, `neutral_cluster_bonus`, and `neutral_border_bonus`.
- Enemy intel logging: Target lines mark enemies as “known” only when tracked power exists (tracked armies or garrison memory); first-turn unseen enemies remain unknown.
- Recruitment: When no recruit castle is available (or unreachable via friendly-only paths), the army logs the miss and spends the turn camping/defending; recruitment moves route strictly through owned regions (no hostile hops) when heading to a castle or peasant pickup.
- Economy logging: AI log now records castle build/upgrade candidates with scores plus resource gaps and top-up attempts when builds/upgrades are skipped.
- Log ordering: AI turn intro/header is emitted before pre-move economy execution so direct economy logs (trade/bank/top-up) are grouped under the correct player section instead of appearing at the previous player tail.
- Movement debug logging: target candidate dumps now print top 10 scored candidates (instead of top 3) for easier AI target-selection tracing, and each candidate line includes required `mp_cost` and `distance_penalty` before the score component breakdown.
- Movement debug logging now also prints `Rejected candidates` with explicit filter reasons (overmatched, no friendly path, raider thresholds, merge halt, unreachable this turn) so dropped targets are visible in AI logs.
- For `known_overmatched_filter` rejections, logs now print known enemy army `name:power` details (no score), e.g. `enemies: Army II:56, Army I:34`.
- Signals: `turn_started`, `move_prepared`, `move_started`, `battle_started`, `battle_finished`, `region_conquered`, `turn_finished`.
- Uses: `ArmyPathfinder`, `ArmyTargetScorer`, `BudgetManager` (allocate budgets at turn start), `RecruitmentManager` (hire at castles when requested; AI allows instant hire for `just_raised`), `DebugStepGate`.
- AI castle upkeep: TurnController now runs economy in two phases with one `EconomyAIManager` instance: pre-move economy with emergency reserve lock, then post-move castle threat refresh + castle recruitment execution.
- Castle upgrade bank: `EconomyAIManager` deposits 10% of spendable gold at AI turn start (turn 2+) into `Player.ai_castle_upgrade_savings_gold`, hard-locks that gold from non-upgrade spending, and caps the bank at `Stronghold gold cost * 1.25`.
- Castle reserve budgets: threatened-castle reserve requests now use the same base allocation weight as armies (`1.0` per request), and each castle reserve is capped by recruits (`max gold = 10 * recruits`, `max wood = floor(0.5 * recruits)`, `max iron = floor(0.5 * recruits)`), applied in both pre-move and post-move reserve recalculation.
- Threatened-castle movement tree: armies below the minimal recruitment threshold inside their own threatened castle request recruitment and route to recruitment before threat decisions, bypassing threat-locked local camping for that pass. Other threatened-castle actions use the pre-move snapshot (`threat_level >= 2`) before normal recruitment and role scoring. After AI conquest (`battle_victory`, or `battle_withdrawal` with defender-side withdrawal and captured owner now AI), TurnController refreshes the in-turn threat snapshot so follow-up castle targeting does not use stale pre-conquest threat data.
- Home-castle threat handling groups threats per enemy region while preserving known total power, unknown-risk, source ids, castle target state, attack cost, and return cost. Known attacks require `>=120%` and pull only the minimum garrison needed, capped at `150%`; unknown non-castle attacks require same-turn attack+return; mixed known+unknown groups remain unknown-risk. If the castle can defend itself, or can be made safe by transferring a minimal garrison from the army while the army stays at or above its minimal recruitment threshold, the army is released to normal role scoring instead of camping.
- Non-home threat defense order remains `direct attack -> local same-region merge check -> support merge -> castle garrison sortie -> reposition fallback`; sortie can trigger for unknown castle threats (without ratio-based garrison pull), still uses known-threat power-ratio gates when intel exists (`>=120%` feasible, pull only up to `<=150%` target ratio), and for multi-threat castles requires same-turn attack+return MP feasibility before committing.
- Army loop first-step: when an army starts a decision cycle inside a castle with `castle_nearby_entities["enemy_army_ids"]` empty and no directly adjacent enemy-owned regions, it absorbs the full local garrison into itself before threatened-castle/recruitment/role logic.
- Wounded-heal pre-scoring check: after threatened-castle/castle-hold/recruitment steps and before role scoring, armies run `_check_if_heal_wounded()`; if wounded ratio (`wounded / (active + wounded)`) meets difficulty threshold (Easy 50%, Normal 35%, Hard 20%) and MP remains, they repeatedly camp to heal until ratio drops below threshold or MP reaches zero.
- Threat power refresh: threat attack/support/defense checks refresh each threat entry with live tracked enemy power during movement, so known-power comparisons use up-to-date memory instead of stale pre-move snapshot values.
- Two-mode role routing: after threat/recruitment checks (peasants-only cycle is temporarily disabled/commented), armies follow a per-turn role assignment. `EXPANSION` mode (no enemy frontier) sets all armies to `RAIDER`; `WAR` mode uses a hard-target staircase based on valid in-range MAIN hard goals (enemy army/castle within MAIN range and safe route) to assign `MAIN`, computes `RAIDER` slots from total frontier size with cap `ceil((hard+soft)/4)`, and marks leftovers as `SUPPORT`. If no `MAIN` is assigned, remaining armies stay `RAIDER` (no support conversion). Targeting uses two buckets: `hard` (non-owned region with an army or castle, including neutral castles/outposts) and `soft` (no army/castle, neutral or enemy-owned). `MAIN` prioritizes hard→soft, while `RAIDER` uses one combined scored pool (soft + hard) with `mp_cost * AI_MAIN_GOAL_DISTANCE_COST_MULTIPLIER` distance penalty, never merges for soft targets, and applies hard-target exceptions only (known weaker army by 20% or known castle defense by 2x). `SUPPORT` merges to nearest reachable main, else routes to recruitment, else camps.
- Raider unknown-hard gate: for hard targets with unknown enemy strength, raiders no longer always reject; they roll a difficulty-scaled attack chance (Easy 25%, Normal 50%, Hard 75%) before continuing normal merge/attack flow.
- Raider castle scouting: on Normal and Hard, `RAIDER` armies adjacent to an enemy castle with unknown tracked garrison power attack it before normal scored raiding, using standard AI battle/siege handling to reveal intel.
- Main routed goal selection: before normal hard/soft frontier targeting, `MAIN` armies score non-owned goals within a difficulty range (Easy 2, Normal 3, Hard 4 hops), log top ranged goal candidates/rejections, then use the same normal passable route execution for the selected goal. MAIN ranged scoring adds +5 for same-turn reachable goals, doubles path-cost penalty, uses +10 castle bonus, stacks x3 when the AI owns no castle and another x3 when the castle is winnable with full known defenders, and doubles pursuit bonus for known enemy armies beatable at 120%. If no ranged route is handled, MAIN selects the normal hard/soft frontier objective and tries the same routed first non-owned step. Routes reject intermediate castles and known enemy-army steps that fail the existing 120% attack ratio; unknown intermediate armies are rejected only on Easy.
- Main hard-target reachability: `MAIN` prioritizes reachable hard targets first; if none are reachable this turn, it can still advance toward the best unreachable hard target instead of immediately falling back to soft targets.
- Live frontier refresh: role behavior refreshes frontier buckets before each army decision pass, so newly conquered regions immediately affect hard/soft target pools within the same turn (roles remain turn-assigned).
- Role move safety: even though role buckets are turn-scoped, target selection revalidates live ownership/bucket state and skips the army’s current region so captured regions cannot be repeatedly selected with zero-cost self-moves.
- Threat defend de-dup: castle fallback reposition now runs a live sufficiency gate so armies do not pile onto the same castle. Unknown threats allow only one defender unless none is present; known threats allow additional defenders only while known threat power exceeds current full castle defense (`armies + garrison + recruits`), and reposition is reach-this-turn only.
- Unknown-threat castle hold/reposition now uses a minimum defense floor derived from the same minimal recruitment-threshold formula (`base_roll=10`, turn-scaled, peasant-power based): level-3 unknown holds are released when current castle defense meets the floor; otherwise the defender stays and castle recruitment can continue. Home-castle repeated holds can release after leaving a minimal safe garrison if the same unresolved threat signature persists.
- Wealth tracking: caches start-of-turn resources for AI log headers, writes the final totals after movement, and calls `PlayerManagerNode.update_player_wealth_status()` so every player ends their turn tagged as poor/normal/rich (thresholds in `GameParameters`).
- Battle Logs: After `_log_army_move_result()` records `"Fight at ..."` lines, `_log_recent_battle_details()` pulls the queued summary from `GameManager.consume_ai_battle_log_for_army()` so AI logs show `Battle started`, attacker/defender stats, the result string (`Won/Defeated/Withdrew`), and the post-battle status lines right after the arrival entry; battle log tokens are captured before travel so freed armies can still retrieve their queued entries, and the header uses the actual contested region returned by `ai_travel_to`.
- Recruitment Loop: Recruitment movement uses a state machine per army (`NORMAL`, `TRANSFER_TO_CLOSEST`, `RECRUIT_OR_DEFEND`) with route resolution per decision pass: it logs `Reachable recruitment castle candidates` first (friendly-only multi-turn paths), then `Breakthrough recruitment castle candidates` (non-friendly paths filtered by bounded clean-breakthrough rules: max 120 explored nodes, max 3 non-friendly regions, and no armies/castles on intermediate non-friendly regions). Fallback transfer/camp runs only when both lists are empty (or breakthrough is blocked by below-half threshold). `RECRUIT_OR_DEFEND` no longer recurses back into `NORMAL` in the same pass, preventing duplicate castle-pick loops. After a successful recruitment-transfer merge, the donor army spends remaining MP on camp to prevent same-turn local-transfer loops.
- Withdrawal Logging: `battle_withdrawal` results note which side withdrew; defender withdrawals are treated as holding the target (no extra movement log), while attacker withdrawals log the retreat destination.
- Siege withdrawal: AI attackers withdraw during castle battles once assault effectiveness is 0% and no active or reserve rams remain.
- Post-withdrawal reposition: when an AI army withdraws or loses and still has movement points, it moves to the strongest reachable friendly region within range.
- Failed AI castle siege simulations record the scouted defender presence before pre-battle withdrawal, so raider scouting does not keep treating the same castle as unknown.
- Calls back into `GameManager.ai_travel_to()` for step-wise movement and battle triggers.
- Reinforcement routing: Armies that need recruitment but have no assigned budget now queue a `goal: reinforce` move toward the best owned castle before chasing frontier targets, so low-power stacks travel home for resupply.
- Castle targeting: When enemy strength is known and the target has a castle, `_build_enemy_info()` triggers `GameManager.simulate_siege_battle()` and `_evaluate_merge_policy()` halts attacks predicted to lose (or withdraw) and proceeds when the sim reports victory.
- Army loop (AI only): per-turn armies are shuffled, then each army loops until MPs are gone — quiet-castle garrison release, under-strength threatened-castle recruitment, threatened-castle handling, castle-hold, recruitment, wounded healing, then role-driven action (`MAIN`/`RAIDER`/`SUPPORT`). If no valid role action remains, the army camps out remaining MP. The loop now hard-stops stage evaluation as soon as MP reaches 0 in-pass, preventing extra scoring/target selection after exhaustion.
- Resting Logic (AI): armies rest to 100% vigor before any move if efficiency < 90% (camp until full or MPs run out), and any time no move is possible they burn remaining MPs camping; end-of-turn pass still exhausts leftover MPs with `make_camp()`.
- Peasants-Only Recruitment:
  - Temporarily disabled/commented in the AI army loop.
  - When re-enabled, runs only when standard recruitment is not needed, the player has positive net food growth, and the army is below its wealth-adjusted peasant floor (Poor uses the base 20% minimum, Normal halves to 10%, Rich players skip peasants entirely).
  - Targets peasant share based on army power thresholds, scaled by wealth (Poor uses 40%/30%/20%, Normal halves those shares, Rich uses 0%).
  - Filters candidate regions to those with enough recruits to meet the calculated need, then picks the best recruits-per-(distance+1) ratio so nearby pools are favored.
  - If no region can meet the minimum, the army flags for normal castle recruitment; otherwise it moves/hire peasants for free using standard pathfinding within current MPs.
- AI Logging: During AI turns `TurnController` calls `AILogManager` to write the requested log format — each turn header includes resources, signals, decision, and owned regions, while every army action logs as `Recruits`, `Goes to Castle`, or `Moves` with its target plus recruitment/economy success or failure reasons.
- AI intro logging now prints the current player castle-garrison list immediately before `______ ECONOMY ______` using army-style power/composition lines, e.g. `Keep Northvik [Power: 168 - P:29, S:13, A:7, SW:7, H:1, K:1]`.
- Decision-tree logging: movement decisions emit explicit branch logs for threat handling and role routing (including mode/role selection, raider hard-target exceptions, and support merge/recruit/camp outcomes) via `AILogManager.log_army_decision_tree(...)`.
- AI log entries now show efficiency inline (`Power: 67, E: 95 - P:...`) so it’s easy to see when armies act below full effectiveness.
- Frontier targeting logs the top 10 scored candidates (name/id, score, resources/pop/level/castle/pursue/neutral bonuses/strategy components) before selecting the best attack target.

### BattleManager.gd (BattleManager)
- Purpose: Battle UI coordination and post-battle processing via signals.
- Key: `start_battle()`, `handle_battle_modal_closed()`, emits `battle_finished`; tracks pending conquest; proportional loss application; removes only battle-participant armies reduced to zero (no global scan).
- Integrates: BattleModal UI, RegionManager (read), ArmyManager (remove/retreat), GameManager (finalization).
- Behavior: If `GameManager.debug_disable_battle_modal` is enabled for AI, `start_battle()` simulates instantly via `BattleSimulator` and emits `battle_finished` without showing a modal. AI withdrawal checks include all defenders (armies + garrison + recruits), scale chance from `AI_WITHDRAW_POWER_THRESHOLD` down to forced retreats at `AI_WITHDRAW_MAX_POWER_DIFFERENCE`, and defender armies can retreat pre-battle (each picks a random owned neighbor) which ends the battle immediately.
 - Withdrawal: `BattleSimulator`/`AnimatedBattleSimulator` carry `withdrawing_side` in the BattleReport; `GameManager.finalize_battle_result()` applies retreats based on that. If missing, it falls back to attacker withdrawal.
- Defender-withdrawal retreat reparenting notifies `ArmyManager.on_army_moved(...)` for each moved defender so region nearby caches stay synchronized with new regions.
- Withdrawal destinations ignore owned neighbors already holding five armies.
- Post-battle reselection clears `UIManager` modal state so the next region click moves immediately.
- `BattleSimulator.gd` and `AnimatedBattleSimulator.gd`: battle logic now lives entirely in `BattleSimulator` (both background and step-wise sessions); `AnimatedBattleSimulator` drives the UI by consuming session steps and emitting `round_completed`/`battle_finished`/`ai_withdrawal_started`. Session `round_data` now also carries log-only per-attacker casualty breakdown payloads (`attacker_kill_breakdown`, `defender_kill_breakdown`, optional `withdrawing_side`) for BattleModal rendering.
- Siege resolution now builds a per-gate plan from a single breach roll (`q` progress) that schedules gate damage over the rolled turns and applies a weighted ram damage queue (failures always chew through assigned rams), logging plan/tick details under the `siege` debug tag. Gate assault ratio now uses destroyed-gate count (live gate HP) instead of the old raw counter, so only fully destroyed gates boost assault effectiveness. Ram damage application treats near-zero HP as destroyed to avoid lingering 1 HP rams from float rounding.
- Breach chance now scales linearly up when assigned archers are below the minimum bucket, instead of clamping down: e.g., with a bucket of 10 and chance 0.50, 2 archers now use chance 0.90 (interpolated toward 1.0 at zero archers as requested).
- Gate assault ratio again uses the legacy raw accumulation: destroyed gates add 5 raw per turn; the gate contribution is raw/non-ranged (no 10% cap) summed with wall/ladder. Per-round ladder/wall/gate totals and gate raw accumulation (`[GateRaw] destroyed=.. added=.. raw_before=.. raw_after=..`) are logged under `siege`. Destroyed gate detection tolerates HP ≤ 0.01.
- Battle/UI: Siege panels now read gate HP as floats (no truncation), so gate progress shows finer-grained percentages instead of 20% steps.
- ArmyTracker logging traces garrison power/army tracking during battle setup and observation updates; conquered regions now clear all tracked garrison intel via `PlayerManagerNode.clear_enemy_garrison_memory()`.

### Region systems
- region_manager.gd (RegionManager): ownership map, castle start, neighbor graph, resource generation, level upgrades, turn ticks (population growth, recruit replenish, castle builds, ore search reset), frontier discovery, recruit source aggregation, castle level lookup. Registers `VisualManager` via `set_visual_manager()` so ownership changes can clear any temporary hover highlights before recoloring overlays, and receives `ArmyManager` via `set_army_manager()` so ownership flips immediately reclass nearby army sets for that region. `rebuild_region_graph()` refreshes pathing adjacency after scenario terrain overrides. `set_region_ownership()` now marks `Region.just_conquered_this_turn` when ownership changes to a different player (cleared on neutralization), enabling same-turn post-conquest action lock in region UI. Castle-start ownership clears inherited neutral garrisons from the starting region and claimed neighbors, then starting castle-region upgrades use the same recruit-cap bonus rule as promotion. `find_best_recruitment_castle()` returns both the best region id and the scored candidate list, using `(total available recruits from the castle + owned neighbors) / distance * (1 + 0.2 × castle level)`, divides the recruit pool evenly across all armies requesting recruitment at that castle (including the requester) for scoring, adds `+ garrison_power` only when `needs_recruitment_armies == 0` and `castle_nearby_entities["enemy_army_ids"]` is empty, and rejects any castle not connected via wholly owned regions when selecting the best target. Proportional pooled-recruit deduction now uses largest-remainder apportionment so leftovers are distributed by fractional shares instead of being dumped into the last source region.
- region.gd (Region): data container with population, resources, recruits, ownership counter, garrison, castle type/upgrade, ore search, movement/passability helpers, plus precomputed `nearby_regions` and region-level cached nearby entities in `castle_nearby_entities` as ID sets: `army_ids`, `castle_ids`, `friendly_army_ids`, `enemy_army_ids`. Provides `clear_garrison()` for flows that convert neutral starting territory to owned territory without combat. Resource amounts are derived from base resources with `round(base * (1 + 0.375 * (level_number - 1)))`. Promotion and explicit promoted-level setup immediately add the recruit-cap increase to available recruits, clamped to the new cap. `get_growth()` applies a permanent level bonus (`(level_number - 1) * 0.005`) plus food/const terms and neutral-only penalty (`-0.01`), while recruit replenishment uses `base_rate + level_bonus_rate` where `base_rate=RECRUIT_REPLENISH_RATE` (`0.006`, multiplied by `PROMOTION_REPLENISH_BONUS_MULTIPLIER` during active promotion replenish turns) and `level_bonus_rate=(level_number - 1) * REGION_LEVEL_REPLENISH_BONUS_RATE`; replenish also carries fractional leftovers between turns (e.g. `0.9 + 0.9 -> 1 recruit + 0.8 carry`). Castle repair cost scales by defense-damage fraction against half of build cost (`ceil((build_cost / 2) * fraction)` per resource).
	- `apply_population_loss(loss, min_population)` clamps population and recalculates recruits.
	- `heal_all_wounded()` now restores wounded recruits straight into the base recruit pool (pre-ownership modifier) so they heal at turn start without touching population.
	- `fill_recruits_to_maximum()` resets the recruit pool to the region's current cap (used during scenario setup).
	- `apply_siege_damage()` uses `GameParameters.CASTLE_WALLS_GATES` to drive wall/gate counts and per-section HP; gates and walls store individual HP arrays; repair now snapshots HP at repair start and, on completion, only fixes that pre-existing damage (damage taken during repair remains); `apply_wall_section_damage()` reduces wall-section HP and reports destroyed/damaged sections without altering the castle defense bonus (single defense value now).
	- Ore search offers three attempts but halts permanently after the first discovery (UI and AI use `can_search_for_ore()` for gating).
	- Tracks per-turn action usage (ore search/raise army/promotion) cleared by `RegionManager.reset_all_ore_search_turn_usage()` at round start.
- Info modal theme uses a `FontVariation` and theme shadow settings for Labels so region stats stay legible over busy backgrounds without outlines.
	- `get_wounded_recruits_total()` exposes the wounded-peasant count for UI consistency.

### RecruitmentManager.gd (RecruitmentManager)
	- Purpose: Builds recruitment plans from ideal compositions, budgets, and recruit caps for armies/garrisons using the same pipeline for simulation and execution.
		- Sandbox helper: `recruitment.py` is a standalone Gaussian visualizer (Tkinter) where mu shifts linearly by gold/recruits (<=0.5 -> 0, >=5 -> 4) across L0..L4 (`peasants`, `spearmen`, `swordsmen/horsemen`, `knights/mounted_knights`, `royal_guard`); horsemen share is `((grassland_global + grassland_frontier) / 2)%` and splits L2 (`swordsmen` vs `horsemen`) and L3 (`knights` vs `mounted_knights`), while ranged weights are split by melee-mass groups and recruitment evaluates multiple ranged targets, then picks the best two-pass result so final recruited units hit 20-30% archers/crossbowmen when feasible (closest best-effort otherwise); tier cap (1..5) still masks higher tiers, and recruit allocation still respects gold/wood/iron budgets.
	- RecruitmentManager.gd now mirrors the Gaussian sandbox: no ideals/upgrades/diversity; global grassland % is cached from all non-ocean map regions, frontier grassland % is computed from current player frontier, L2/L3 buckets split into (`swordsmen`,`horsemen`) and (`knights`,`mounted_knights`) by horsemen share, and recruitment evaluates multiple ranged targets before the two-pass buy so final ranged share lands in 20-30% when feasible (closest best-effort otherwise); tier cap still masks higher tiers, garrisons still block cavalry, and purchase trimming follows sandbox order (wood trims from low-tier order, iron trims from high-tier reverse order) without an extra post-pass gold trim stage.
	- `recruitment.py` unit resource costs are synced to live `GameParameters` values (gold/wood/iron) for apples-to-apples sandbox checks.
	- Sandbox purchase fallback now recomputes per-loop wood/iron usage after forced single-unit insertion so reported `spent` resources always match final hired counts.
	- Current elite gold costs: Knights=6, Mounted Knights=9, Royal Guard=12 (iron costs unchanged).
- Hard caps: Starts from `T` peasants, only converts/upgrades within budget left after that base cost, so total recruited never exceeds the chosen `T`; spending reports use `spent = start - left` for gold/wood/iron.
- Castle tier gating: Ideal mapping and upgrade/burn steps drop unit types above `GameParameters.CASTLE_RECRUITMENT_TIERS` for the current castle, so keeps cannot hire Royal Guards or other over-tier units.
- T chooser tweak: When gold-per-recruit is low, the T picker now gives a small bonus to larger T (quantity) before evaluating diversity/ideal distance; when the ratio rises toward/above 1.0, the bonus fades out so quality spending dominates again.
- Burn-budget guard: Upgrade burn now preserves at least the ideal (rounded) counts for each donor type and skips converting peasants/spearmen when gold-per-recruit is low, keeping cheaper bodies in low-budget situations.
- Scarcity bias window tightened: `RECRUIT_SCARCITY_LOW/HIGH` now 0.5/1.5 gold-per-recruit, so quality bias ramps up as soon as gold/recruits is decent and reaches max when ratio ≥1.5.

### Army systems
- army_manager.gd (ArmyManager): create/select/move/deselect armies, movement points reset, battle hooks, tracking/cleanup, plus event-driven region-level nearby cache synchronization (ID sets for armies/castles and owner-relative friendly/enemy army sets) across create/move/remove/castle-presence/ownership-change events. Maintains a runtime `army_by_entity_id` index (`army_<instance_id>` -> `Army`) for O(1) entity-ID resolution used by nearby-cache/threat consumers. Exposes custom target-arrow/highlight helpers for SpawnEvent manual placement (`show_custom_target_arrows`, `clear_custom_target_arrows`) reusing the standard arrow/highlight visuals. `calc_reinforcement_threshold(turn_number)` caps turn at 20 and uses `random(10..20) * (1 + 0.03 * turn) * peasant_power * 2`.
- army.gd (Army): movement points, ownership, efficiency (reduce/restore, affects combat), recruitment request/assigned budget, recruitment movement state (`NORMAL`, `TRANSFER_TO_CLOSEST`, `RECRUIT_OR_DEFEND`), and simple power checks. `get_recruitment_threshold(turn_number, roll, minimal, maximum)` caps turn at 20; `roll=true` rerolls `random(10..20)`, `minimal=true` forces base 10, `maximum=true` forces base 20. `needs_recruitment(...)` should reroll only at army turn-start refresh and otherwise use flag-driven checks with `roll=false`. First-turn universal recruitment is handled by `EconomyAIManager` pre-move candidate selection only. Emits `movement_points_changed(army, new_points)` whenever points reset/spent/camped so ArmyManager can refresh highlights. Includes `just_raised` flag (AI-only instant recruitment bypass on first turn; cleared after use), instanced warrior visuals (`scenes/warrior_1..6.tscn`) scaled only by map size, and `get_display_name()` to standardize log/UI labels (falls back to the roman number when the node keeps a default Node2D name). When MP hits 0 and `kneel`/`rest` animations exist, Army plays `kneel` then loops `rest` on the `Victory` sprite layer; on human End Turn, reverse-kneel stand-up is applied only to armies that were already exhausted (0 MP) before auto-camp for that turn, while auto-camped armies can suppress the next turn-start stand-up transition. Exhaustion transform tuning is now data-driven with a per-warrior array (`EXHAUSTION_VICTORY_TUNING_BY_WARRIOR`) that provides separate `kneel`/`rest` scale-bonus and offset values.
- ArmyManager exposes `transfer_all_soldiers(donor, receiver)` for bulk transfers, and armies keep `spawn_minimal_peasant_token()` so TurnController can leave placeholder peasants after merging stacks.
- ArmyPathfinder.gd: pathfinding within MP horizon, multi-turn pathfinding with MP constraints.
- **ArmyTargetScorer.gd**: **Single source of truth for army region scoring**. Provides canonical raw scoring APIs:
  - Ownership bonus includes base enemy-region bonus plus a difficulty-based modifier only when AI targets a human-owned region (`-5/0/+5` on Easy/Normal/Hard).
  - `get_base_region_score_raw(region, player_id)`: Returns raw base sum and components (strategic + population + level + resource, no normalization)
  - `get_resource_component_breakdown_out_of_10(region, player_id)`: Per-resource contributions that sum to resource component  
  - `get_final_army_score_raw(army, region_id)`: Returns final_raw = base_raw + random - mp_cost with full breakdown
  - Pursuit bonus: when pursuit ratio is met, score bonus is doubled if the target has a known enemy army that is isolated (no adjacent region owned by that enemy owner), making focus-destruction targets more attractive.
- ArmyMovementPlanner.gd: High-level army movement coordination using canonical raw final scoring from ArmyTargetScorer. Applies danger penalty as separate adjustment after raw final score.

### VisualManager.gd (VisualManager)
- Purpose: Place/update/remove castle visuals; create army visuals via ArmyManager; trigger region visual updates. Region highlights reuse `animate_region_highlight_on(region_id, params)` / `animate_region_highlight_off(region_id)` for move targets and global hover; movement uses `GameParameters.REGION_ANIM_*` alpha pairs (owned 0.5↔0.75, neutral 0.35↔0.55) with hover alphas (owned 0.85, neutral 0.55). Passing `animated=false` applies map-hover values from `GameParameters.REGION_MAP_HOVER_*` (owned base 0.6→0.7 hover, neutral base 0.2→0.3 hover). `set_map_hover_region()` uses those toned-down values on non-ocean, non-mountain land; movement highlighting remains animated while `_ready_highlight_regions` restores turn-start highlights after hover/move overrides, all animated overlays share a synced tween cycle so new ready regions match existing pulses, and ready-army highlights suspend whenever move-target overlays are shown so only legal destinations animate.
- `clear_interaction_highlights()` drops both move-target pulses and map-hover overlays when UI modals take over (ArmyManager/GameManager/ClickManager call it so contested neutral regions don't keep pulsing after retreats/withdrawals).

### map_editor.gd (MapEditor)
- Purpose: Map editor mode controller for toggling editor UI.
- Key: `initialize()` — shows MapEditorPanel, hides player/turn UI.
- Region edits: `region_type_changed` updates both the in-memory `Region` node and `MapGenerator`'s region dictionaries, then calls `refresh_region_visual()` and reruns `BorderManager.setup()` + the noisy polygon/overlay rebuild helpers so switching between grassland/ocean/forest immediately rebuilds coastlines and removes stale borders without touching `map_generator.gd`.

### map_editor_panel.gd (MapEditorPanel)
- Purpose: Left-hand editor UI for per-region edits, player settings, and persistence.
- Scene: `scenes/map_editor_panel.tscn` keeps the Scenario tab content inside a scroll container so large victory-condition/resource layouts remain accessible.
- Key: `Save Map Data` now resolves the active `MapGenerator.data_file_path` through `_resolve_mapdata_path()` before reading/writing so edits always update `res://mapdata/<file>.json` (modification timestamps change as soon as the button is pressed).
- Region edit commit: pending Region-tab line-edit values (name/resources/population/ore guarantee attempt) are explicitly committed before region switches and right before `Save Scenario`, so values persist even when the user does not press Enter.
- Region resource commit deduplicates unchanged values before emitting `region_data_changed`, preventing unnecessary region visual regeneration on click/selection changes.
- Signal contract: `region_type_changed` is now emitted only for actual region type/biome selection; all other region mutations (name/level/castle/resources/ownership/ore/army toggle) emit `region_data_changed`.
- Event editor: `Select regions` toggles map-pick mode for `Region IDs`; clicking map regions adds/removes IDs in the comma-separated field, pressing `Esc` or pressing `Select regions` again exits pick mode, and while an event editor is open the listed region IDs are shown with animated highlight pulses (same visual style as move targets).

### PlayerManagerNode.gd (PlayerManagerNode)
- Purpose: Player storage and per-player economy as a scene node.
- Key: initialize players; `current_player_changed` signal; add/remove/afford/pay; per-player income `process_resource_income_for_player()` (AI non-gold income uses `GameParameters.AI_RESOURCE_GROWTH_BONUS`; AI gold income uses difficulty bonuses: Easy +0%, Normal +5%, Hard +15%); total army+garrison food cost; `set_player_resources()` for scenario starts; save/load.
- Upkeep helpers: region level upkeep is driven by `GameParameters.REGION_UPKEEP_COSTS` (`L3=1W`, `L4=1W+1S`, `L5=1F+1W+1S`); castle upkeep is driven by `GameParameters.CASTLE_UPKEEP_COSTS` (`Outpost=2W`, `Keep=1W+1S`, `Castle=1W+2S`, `Stronghold=4S`); aggregate upkeep is used by turn-start deductions and projected economy growth.
- Exposes `get_player_food_growth()` and `meets_food_upgrade_safeguard()` so UI/AI can gate promotions on the required post-upgrade food buffer from `GameParameters.AI_MIN_FOOD_AFTER_UPGRADE`.
- Wealth helpers: `get_player_wealth_level()` / `update_player_wealth_status()` wrap the new gold thresholds so other systems can react to poor/normal/rich states without reimplementing the math.
- Enemy intel helpers expose tracked enemy power plus last-known army/garrison compositions and army intel age in turns for InfoModal.
- Used by GameManager and TurnController.

### Economy & Recruitment
- BudgetManager.gd: Shared recruitment allocator for both army and castle requests. Splits one combined gold/wood/iron pool across entries (weighted; deterministic largest-remainder), then splits local recruit availability per castle across all entries in that castle; sets `army.assigned_budget` and request `assigned_budget` with `BudgetComposition`.
- BudgetComposition.gd: Simple value holder for gold/wood/iron/available_recruits; afford/check helpers.
- RecruitmentManager.gd: Python-inspired recruitment (feasibility-picked T with spend target, ranged/peasants bounds, diversity floor, scarcity bias/boosts, low-gpr quantity bonus) using `GameParameters` exports (`RECRUIT_*`, unit boosts) and per-castle/garrison ideals; supports non-mutating army previews plus committed army plans so scarce-budget simulation and final hire use the same counts; budgets cannot go negative, and spent resources are `start - remaining`.
- PlayerManagerNode.gd: `get_player_economy_snapshot()` aggregates balances, projected income (with food upkeep), and population totals; `get_projected_economy_for_player()` wraps it for compatibility and UI (PlayerStatusModal2) consumption.

### EconomyAIManager.gd
- Purpose: Central, deterministic AI economy planner with event-driven castle-threat reserve cache.
- Key Turn APIs:
  - `run_pre_move_economy_with_reserve_lock(player_id, turn_number)`: computes signals, analyzes castle threats from `castle_nearby_entities`, builds one shared pre-move recruitment pool for armies + threatened castles, syncs castle reserve lock from that pool, executes early surplus-selling, deposits AI castle-upgrade savings bank (turn>1), evaluates castle upgrades, then runs remaining pre-move economy actions.
  - `run_post_move_castle_recruitment(player_id, turn_number)`: refreshes castle threats after movement, resimulates known threats when needed (unknown->known or power change >15%), recalculates weighted reserve budgets, and executes castle recruitment.
  - One-turn threat reachability treats intermediate castles owned by non-enemy players as blockers (target castle remains enterable for evaluation), preventing threats from being counted through hostile/neutral chokepoint castles.
  - `analyze_castle_threats_pre_move(player_id)`: public pre-move threat scan hook.
  - `get_castle_threat_snapshot(player_id)`: read-only deep-copy snapshot for movement decisioning (`threat_levels_by_region`, `threat_registry_by_region`).
- Threat model:
  - Per-castle threat registry keyed by `army_<id>` with `known/power/region_id/threat`.
  - Threat classes: `castle_safe` (known attacker cannot beat castle-alone), `needs_army` (known attacker beats castle-alone but loses vs full defenders), `unknown` (unknown power), `big` (known attacker beats full defenders).
  - Values: `castle_safe=+1`, `needs_army=+2`, `unknown=+2`, `big=+4`; `threat_level` stores max severity `0..4`.
  - Reachability filter: enemy IDs from `castle_nearby_entities["enemy_army_ids"]` are treated as threats only if pathfinding confirms they can reach the castle within one turn (`cost <= MOVEMENT_POINTS_PER_TURN`) using `friendly_only=false`.
  - Threat scan visibility: each castle summary logs all nearby enemy cache entries and marks rejections with reason (`not_in_tracking`, `no_path`, `insufficient_mp`) and computed path cost.
  - Known enemy armies in the same region are merged into one simulated attacker and classified with two simulations (`sim_castle_only`, `sim_with_army`) to derive level 1/2/4.
  - Threat logging: pre/post threat summaries include per-threat detail lines under each castle (`id`, source region, `threat` label/level, `intel` known/unknown, tracked power, `sim_castle_only`, `sim_with_army`) for debugging classification.
- Reserve model:
  - Only castles with `threat_level >= 2` receive emergency budgets.
  - Pre-move recruitment uses one shared budget pool across army and castle requests (castle request weight = `total_threat_value`, army weight = 1.0).
  - When multiple castle armies wait for recruitment, the army-only share is re-simulated and pruned so as many armies as possible reach the minimal recruitment threshold; deferred armies keep their request, while the final single candidate always keeps its budget even if still under minimum. Castle/garrison request budgets remain unchanged.
  - Reserved resources are synced from castle shares and hard-locked during pre-move economy spending checks.
  - Separate AI-only upgrade-bank gold lock is applied to all non-upgrade spend checks (recruitment/raise/build/ore/food-buy).
  - Separate AI-only raise-reserve gold lock is applied to all non-raise spend checks (recruitment/build/upgrade/ore/food-buy); raise path can consume this reserve.
- Trade ordering: AI trade is split into `sell_surplus` (before castle-upgrade checks) and `buy_food_deficit` (after all other economy actions).
- Castle-upgrade savings bank: from turn 2 onward, AI deposits 10% of spendable gold into persistent per-player `ai_castle_upgrade_savings_gold`, capped to one Keep->Castle gold cost; successful castle upgrades consume bank gold first in accounting logs. If castle upgrade evaluation finds no viable candidates (`no_upgrade_candidates`), the bank is released (set to 0) so gold is no longer locked.
- Raise-army reserve bank: AI accumulates persistent per-player `ai_raise_army_savings_gold` up to `RAISE_ARMY_COST` using partial carry-over (`add = min(cap - current, spendable_gold)`), when raise intent exists (`candidate`, `armies == 0`, or a non-full level 2+ castle can become a future raise source) and no severe castle threat (`threat_level < 2`); reserve is consumed by successful raise, held while `Raise Army` returns `waiting_for_recruits`, and released when no future raise source remains. Raise gates are difficulty-aware when there is no severe castle threat: Easy keeps 40 recruits / 30 reserve / 0.50 score, Normal uses 30 / 20 / 0.45, and Hard uses 25 / 0 / 0.40; severe threat reverts to the Easy/conservative gates. Raise is skipped at the selected castle when a friendly army there already has a recruitment request.
- Integration: TurnController now runs this manager in two phases (pre-move + post-move) within one AI turn instance.
- Recruitment budget safety: pre-move economy now clears stale recruitment flags on armies that no longer need reinforcement, re-runs allocation for any still-needing castle army that ended up without `assigned_budget`, and applies a non-null fallback budget object to prevent recruitment wait loops.
- Army recruitment ownership guard: pre-move army recruitment/budget assignment is now restricted to armies standing in player-owned castle regions; no army recruitment is performed from enemy/neutral castles.

- Priority Flow:
  - If any armies at castles need recruitment this turn → allocate recruitment budgets and skip raising/building.
  - Else try to raise an army; if raised → allocate recruitment for the new army and skip region spending.
  - Else defer region economy to post-movement phase.
- Castle building (post-move): runs a no-side-effect Outpost affordability/top-up preflight before candidate scanning, then scans owned regions without castles, scores each as `strategic_heatmap*10 + neighbor_support` (friendly neighbors +15, neutral +5, enemy -10, and `-45` if any neighboring region already has a castle). Castle regions themselves are excluded; neighboring-castle regions are penalized (not hard-blocked) so high strategic value can still win. Candidates below 100 points are rejected before forward-distance BFS checks. A passing candidate must also be closer to at least one enemy castle than any current friendly castle (BFS distances cached per map). Winning region starts an Outpost build immediately; AI debug logs cap economy candidate lists to the top 10 entries.
- Castle upgrades: evaluates every owned castle not already upgrading. Score = normalized recruit availability (total recruits from the castle plus neighbors) + normalized proximity to enemy castles. The closest/highest-recruit castle gets 100 per axis; others scale proportionally. The best affordable candidate upgrades to the next tier (Keep → Castle, etc.) while logging the full evaluation list.
- Region upgrades: continuously promotes the best non-max-level regions while the player can afford the promotion cost. Score = (castle level ×2) + population/100 + neighboring friendly castle levels + recruit status bonus (empty +2, partial +1, full −2) − current region level. Promotion candidates are rejected when L3 would be reached with wood or food growth below `AI_REGION_PROMOTION_MIN_RESOURCE_GROWTH`, or L4 would be reached with stone growth below that threshold. Every successful promotion logs `"Upgrading <region> to <level> (score: X)"`.
- Region upgrades call `PlayerManagerNode.meets_food_upgrade_safeguard()` so the AI skips promotions that would drop projected food (stock − food cost + growth) below `AI_MIN_FOOD_AFTER_UPGRADE`.

#### Raise Army AI (implemented)
- Goal: Prefer fewer, stronger armies over many weak ones; raise only when we have support and coverage gaps.
- Decision flow:
  1. `pick_best_raise_region()`: Find best castle (level ≥ 2) with enough recruits (≥ AI_MIN_RECRUITS_FOR_RAISING)
  2. `should_raise_army()`: Normalized model over 4 factors via `ai/RaiseArmyDecision.gd`, returns decision + formatted score text for logging
  3. `execute_raise_army()`: Create army and deduct cost
- Gates:
  - Recruits: `recruits_total >= AI_MIN_RECRUITS_FOR_RAISING` (skipped for first-army bootstrap when `armies == 0`)
  - Gold reserve: `player.gold - RAISE_ARMY_COST >= AI_RESERVE_GOLD_MIN`
  - Capacity: ignores castles already hosting `GameParameters.MAX_ARMIES_PER_REGION` armies; execution aborts (with gold restored) if a cap race happens mid-action.
- Logging: decline reasons now include concrete gate/threshold values (for example gold reserve after raise, recruits minimum, frontier pressure minimum, or score below threshold) in the `Raise Army:` economy line; `Frontier2Army` score text uses a guarded ratio (`0.0` when army count is zero) to avoid division-by-zero crashes.
- First-army rule: when `armies == 0`, raise decision score returns pass (`1.0`) if the raise cost is affordable, so AI can bootstrap at least one army; reserve/frontier guards are applied once at least one army exists.
- Scoring factors (normalized 0..1):
  - Region/Army ratio within band `[AI_RAISE_R2A_BAND_MIN..MAX]`
  - Average MP distance of armies to candidate castle `[AI_RAISE_DIST_MIN..MAX]`
  - Recruits and Gold (bank) above minimums
  - Raise-candidate selection now also adds `free_garrison_bonus` to candidate score when that castle has no recruitment-requested friendly armies in-region and no nearby enemy army IDs in `castle_nearby_entities`; bonus is scaled as `garrison_power / 4`.
  - Weights:`AI_RAISE_W_R2A`, `AI_RAISE_W_DIST`, `AI_RAISE_W_RECRUITS`, `AI_RAISE_W_BANK`
  - Threshold: `AI_RAISE_THRESHOLD_NORM`
- Execution: One raise per turn max, deterministic tie-break by region_id; Normal/Hard immediately give the raised army a capped local recruitment budget aimed at the minimal power threshold, while Easy keeps the previous delayed recruitment behavior.
- Tuning: GameParameters (`AI_RAISE_*` normalized constants) and `ai/RaiseArmyDecision.gd`

## Logging
### AILogManager.gd (AILogManager)
- Purpose: Writes AI-only log files under `logs/<scenario>_<timestamp>.txt` in the project root so each game session has its own text file distinct from standard debug logs.
- API: `start_new_game_log(label)`, `log_turn_intro(turn, player_label, player_id, resources, signals, decision, region_names, wealth_label, upgrade_savings_gold)`, `log_army_action(name, power, action, target)`, `log_army_separator(army_number, role_label)`, `log_army_decision_tree(army_name, branch, reason)`, plus helpers for recruitment/economy notes.
- Siege logging: records AI siege equipment purchases with SP available, ladders/rams/trebuchets, and breached/damaged wall sections.
- Format: Turn headers (dashes + `[Turn X]` + ASCII digits) are emitted once per turn, then `[Player Y]` with matching ASCII art, full resource list, `Signals:` (frontier_pressure, underpowered_ratio, castle_spacing, bank_ratio, power_gap_norm), `Decision: ...`, `Regions: ...`, followed by `Army N [Power: Z]: Recruits|Goes to Castle|Moves` with `Target: ...` and recruitment/economy rationales.
- Turn intro resources now include `Upgrade Saved Gold` (AI castle-upgrade savings bank) next to the standard resource balances.
- Integration: GameManager creates the logger once per session and exposes it; TurnController logs each AI turn header, army action, recruitment success/failure, peasant hires, and the post-movement economy summary through this manager.

- PlayerManagerNode.set_player_resources(): applies scenario `player_resources` dictionaries to player resource amounts (keys must match ResourcesEnum.type_to_string, e.g., "Food", "Gold").

## AI & Scoring
- ai/region_scorer.gd: Per-region score (population/resources/level/castle/neighbors/distance). Used for castle placement scoring; hidden Gold/Iron count as zero until discovered.
- ai/castle_placement_scorer.gd: Candidate scoring for initial castles with random perturbation; weights favor resources/size over safety (safety saturates beyond ~7 tiles), and hidden Gold/Iron count as zero until discovered.
- **ArmyTargetScorer.gd**: **Single source of truth for army region scoring**. Provides canonical raw scoring APIs:
  - `get_base_region_score_raw(region, player_id)`: Returns raw base sum and components (strategic + population + level + resource, no normalization)
  - `get_resource_component_breakdown_out_of_10(region, player_id)`: Per-resource contributions using simple weighted sum with modifiers (low income × 3, high stockpile × 0.5 for non-gold); hidden Gold/Iron count as zero until discovered.
  - `get_final_army_score_raw(army, region_id)`: Returns final_raw = base_raw + random - mp_cost with full breakdown
  - Resource scoring: contrib_r = qty_r × weight_r × modifier_r. FOOD uses net change (income - upkeep), others use income. Both overlay and tooltip use identical breakdown.
- ai/FrontierTargetScorer.gd: **Finder only**. Identifies frontier regions and uses ArmyTargetScorer raw base API for scoring. No longer maintains separate scoring logic.
- ai/ai_debug_visualizer.gd: On-map overlays using raw final scores. Army mode displays raw scores from best army perspective. Uses canonical raw APIs and caches resource breakdown. Shows "base_raw+random-movement" format.
- region_tooltip.gd: Displays same raw final scores and component breakdown as overlay. Shows per-resource breakdown using cached factors or fallback to raw APIs.
- ai/StrategicPointsHeatmap.gd: Computes strategic point centrality (ocean-border shortest-path centrality) before castle placement and can toggle a heatmap overlay with key `8` in debug mode. Stores normalized `strategic_point_score` on every Region in [0..10], where the highest-traffic region gets 10.0. Initializes with `initialize(region_manager, map_generator)`; runtime now computes/stores these scores for both custom-map starts and scenario starts.

## Battle Simulation
- BattleSimulator.gd: Statistical resolution with efficiency and proportional loss distribution; normal melee applies long-spears against cavalry; mobility-only withdrawal pursuit and opening-volley ranged rounds resolve assigned hits into kills without long-spears modifiers.
- BattleSimulator.gd: When the `BattleCalculation` debug tag is enabled, each auto-resolved round logs hits and losses per unit type with running totals for both sides.
- BattleSimulator.gd: Finalizes each battle with attacker/defender effectiveness stats (hit % per unit type) whenever `BattleCalculation` logging is enabled.
- AnimatedBattleSimulator.gd: Round-based animated presentation and reporting for the BattleModal.
- BattleModal.gd: Action button switches Withdraw/Continue automatically; message label surfaces withdraw/start/end notices.
- BattleManager.gd: Re-selects victorious human armies automatically, but skips re-selection after withdrawal outcomes.
- BattleManager.gd: Castle defense override now drops per breached wall section (full `trebuchet_damage_to_defense`, half when only damaged) and clamps to the castle’s minimum defense bonus.
- PrebattleModal.gd: Displays castle defense with wall damage applied immediately after trebuchet bombardment (full per destroyed section, half per damaged).
- Siege rams: each has 10 HP and size 10 for ranged targeting; defender ranged fire can hit the active rams (one per gate), destroying them and reassigning reserve rams from a global pool to intact gates; rams return to the reserve when their gate falls.
- SiegePanel: shows active ram HP per gate (progress/percent) using `ram_hp` from siege state; reserves still listed separately.
- AI siege prep: buys siege equipment (trebs/rams/ladders) from siege points/wood, and now fires trebuchet bombard (same shot count/chance as player) before battle when trebs are bought, updating wall damage and assault ratios in the siege payload.
- BattleSummaryModal.gd: Remaining-unit colors only show red when forces started with troops; zero-initial rows use grey.
- Siege trait rules: long spears (1), mobility (3), flanker (4), and charge (5) are ignored whenever a castle is present so siege battles use baseline stats.
- Siege prep: siege points now come only from siege-laborer (trait 8) units; ladders no longer lower castle defense and instead add raw attacker effectiveness (5 each) that scales non-ranged attacker hits during sieges (effectiveness is fixed at battle start based on the attacker’s initial non-ranged count and shown in the prebattle/battle assault labels).
- Armor piercing: subtracts `GameParameters.ARMOR_PIERCING_DEFENSE_REDUCTION` from defender defense chance (floored at 0) before the penetration roll; castle avoidance is applied first.
- Assault effectiveness scales the total pool of non-ranged hits once per assaulting side before distributing targets (no per-unit scaling logs).
- Round totals log kill percentages relative to that side's hits (scaled non-ranged + ranged) for the round.
- ArmyTargetScorer.gd: Applies enemy memory adjustments (nullifies targets when tracked enemy armies are ≥25% stronger, adds ±1 per 5% difference up to ±10).
- ArmyTargetScorer.gd: Combines stored army and garrison intel; requires known power for every defending army before applying bonuses.
- ArmyTargetScorer.gd: Adds configurable +5 bonus for enemy-owned regions (`GameParameters.AI_ENEMY_REGION_SCORE_BONUS`).
- ArmyTargetScorer.gd: Known-overmatched attack gate uses difficulty-aware castle/field min-ratio getters from `GameParameters`; difficulty scaling applies only to AI-vs-human target checks (AI-vs-AI stays normal).
 - Withdrawal Rules:
   - Background and animated sims use `BattleSimulator._decide_withdrawal` with rules injected from `BattleManager`: `withdraw_power_threshold`, `withdraw_forced_threshold`, and siege bailout ratio.
   - `BattleManager` resolves those rules from `GameParameters.get_ai_withdrawal_rules(game_difficulty, ai_vs_human_battle)`, so difficulty scaling applies only to AI-vs-human battles; AI-vs-AI uses normal values.
   - Eligibility: attacker auto-withdraw is AI-only; defender auto-withdraw is AI-only, castle must be `NONE`, and at least one defending army with an owned neighboring region (garrison/recruits never withdraw). Human attacker can still click Withdraw; in dual-human battles both human sides have explicit withdraw buttons (`SecondPlayer/Withdraw` for attacker side, main `Button` for defender side).
   - Resolution: free-hit rounds run in-battle (AnimatedBattleSimulator/BattleSimulator). If attackers withdraw, defenders (armies + garrison) hit them; if defenders withdraw, attackers hit only defender armies (garrison stays). Outcome is `Withdrawal` even if garrison/recruits remain.
   - Manual attacker withdrawal still locks defender auto-withdraw and forces the outcome to attacker withdrawal so retreat happens after free-hit rounds.
- `BattleManager.await_finalize_complete()` lets GameManager pause turn processing until asynchronous battle finalization (camera focus, withdrawals, conquer delays) finishes, so the camera never jumps to the next action mid-animation.
- Withdrawal camera pacing: `GameManager.finalize_battle_result()` now waits for `BattleManager._handle_army_withdrawal()`/`ArmyManager.retreat_army_to_previous_region()` to finish, then the AI camera tracks the retreating army itself instead of the destination region, preventing the extra camera hop reported in logs.
- Debugging: enable `Withdrawal` category in `DebugLogger` to trace human withdraw clicks, simulator decisions, and BattleManager finalization fields.
- Additional withdrawal tracing: AnimatedBattleSimulator logs withdrawal start/rounds/finish; BattleManager logs retreat handling; GameManager logs withdrawal finalization/retreat completion.

## Input & UI
- ESC Key: Opens/closes game menu modal (pauses game) when pressed during gameplay. Not available in map editor mode.
- click_manager.gd (ClickManager): Mouse input → region hit tests → delegates to GameManager (castle placement, movement via `perform_region_entry`), InfoModal updates, human battle start; ESC delegates to UIManager’s single-action escape handler (close one eligible modal or deselect once), except in tutorial mode where ClickManager skips ESC delegation so GameManager/GameMenu routing remains authoritative. In editor mode, also hit-tests `Map/Ocean` polygons to select ocean regions. Army move trigger follows `GameParameters` (`Right click` default, or `Left click` from Options): in right-click mode, RMB is move-only (drag-safe: ignores RMB drags), while LMB becomes selection/region-info behavior; region clicks now open InfoModal for both owned and enemy/neutral regions (enemy/neutral shown with intel rules in InfoModal). Tutorial map-click blocking is step-config driven via `block_map_click`; during tutorial `expected_action=region`, region validation runs before move-flow deselect handling so non-target region clicks are blocked, and ClickManager asks TutorialManager whether a step should block wrong trigger-button clicks. During SpawnEvent manual placement mode it intercepts region clicks before normal movement logic and forwards them to `GameManager.try_handle_spawn_event_region_click(...)`, and outside-map clicks no longer deselect the spawned army while placement is active. Mouse motion feeds `VisualManager` both move-target hover and neutral land hover via the new `set_map_hover_region()`, but while cursor is over `IconsModal` or `SpeedModal` it clears hover highlight and hides region tooltip. When `GameManager.debug_mode` is enabled, pressing `Ctrl` prints region nearby cache dumps for the currently hovered valid land region.
- In map-editor mode, number keys `1..6` enable quick ownership paint, `Esc` cancels ownership paint, and when Event `Select regions` mode is active map clicks are consumed by event region picking instead of changing selection/ownership.
- Movement animation: Army moves animate smoothly using tweens (`Army.animate_move_to`) and play the walking animation during movement (speed scales with the move duration). `ArmyManager.move_army_to_region()` preserves global position across reparenting, computes the stacked target, animates to global target, awaits tween completion, then proceeds (ownership, battle checks, UI). All callers (`GameManager.perform_region_entry`, `ai_travel_to`) await move completion, so no new actions start until the animation ends. Retreat/reposition animations now respect the AI Speed modal multiplier (faster when Fast/Very Fast). After victories, the attacking army switches to the `Victory` animation node, then returns to idle on completion.
- Recruitment-step merge hook: during `ai_travel_to`, when a recruitment-requested army makes a friendly step into a region with another friendly army, it transfers into the strongest local army (strongest→weakest ordering), leaves a 1-peasant token, and the receiver immediately rechecks recruitment need (clears request/state if no longer needed).
- ui_manager.gd (UIManager): Central UI state; tooltip coordination; modal visibility control; polls tracked modals (info/move/prebattle/battle/message/trade/recruit/transfer/call-to-arms/next-player/battle summary/game menu) every frame to update `is_modal_active` and hide IconsModal/TurnModal whenever any are visible. ESC now performs exactly one action per press in priority order (if GameMenuModal is open: close it; otherwise Recruitment/Transfer close, then Move cancel/deselect, then Trade close, then army deselect, then InfoModal close if it is the only open modal); during tutorial mode, default UIManager ESC handling is skipped so ESC routing is controlled by GameManager/GameMenuModal only; blocking modals (Prebattle/Battle/BattleSummary) remain until their buttons close them. During SpawnEvent manual placement mode, ESC is hard-blocked to keep placement atomic. Treats mountain regions like oceans for tooltip suppression. Turn modal remains available during army move selection (even with move-overlay suppression), while still respecting modal and suppression flags elsewhere.
- region_tooltip.gd: Shows region information on hover. In debug mode with army target scoring active, displays same scores as overlay using ai_debug_visualizer.get_detailed_scores(). Shows canonical scoring breakdown (base+random-mp) matching overlay display.
- select_tooltip_modal.gd: Tooltip text provider for action buttons. Uses `SelectTooltipModal` when resource costs are shown, `SelectTooltipModalNoRes` for message-only tips, and `SelectTooltipModalUpkeep` for InfoModal promote/build/upgrade upkeep previews; zero-value upkeep resources hide their icon and value.
- Scenes in `scenes/` folder (each with paired script): `army_modal`, `select_modal`, `info_modal`, `region_tooltip`, `battle_modal`, `player_status_modal`, `recruitment_modal`, `call_to_arms_modal`, `transfer_soldiers_modal`, `transfer_select_modal`, `select_tooltip_modal`, `message_modal`, `next_player_modal`, `game_menu_modal`, `save_game_modal`. Recruitment / TransferSoldiers / CallToArms Continue buttons now close themselves and drop modal mode (no select-modal restore).
		- game_menu_modal.gd: In-game menu accessed via ESC key; pauses game. Button order is Continue (resume), Options, Load Game, Save Game, Exit Game (return to main menu). Options shows the shared `Options` panel (`scenes/options.tscn`) and Back returns to the pause menu button list. In tutorial mode, Load/Save are disabled.
		- trade_modal.gd: UI-only; delegates pricing/affordability to TradeManager and applies trades through it.
		- TradeManager.gd: Centralizes trade math (dynamic pricing, affordability, batch apply) with simple `buy`/`sell` helpers; updates player resources, gold, traded volumes, and emits player status refresh.
		- call_to_arms_modal.gd: Uses `Region.add_call_to_arms_recruits()` so gathered recruits add to population and can exceed the previous recruit cap in the target region.
		- click_manager.gd: Clicking a region opens InfoModal for that region; enemy/neutral output is handled by InfoModal intel mode.
- tutorial_manager.gd / tutorial_modal.gd: Data-driven tutorial steps (welcome message → click castle with world-anchored arrow under `Map/TutorialWorldArrow` → choose army with screen-anchored Arrow2 under `UI/TutorialModal`); step `message` values use translation keys (`tutorial_step_1..tutorial_step_66`) resolved via `tr(...)`, and messages containing `{move_click_hint}` get a runtime hint string based on current move trigger setting (`tutorial_move_click_hint_left/right`); region steps can declare `enforce_move_trigger_click` so wrong trigger-button clicks are ignored while an army is selected; steps can declare `block_map_click` to ignore all map clicks and force the expected UI button interaction; arrows are pre-placed and toggled per step while clicks/UI actions are gated to the expected target; tutorial exposes `is_expected_action(...)` for step-specific systems (camera/input gating); UI steps support completion hooks (`on_complete_deselect_army`, `on_complete_close_info_modal`) that run after step advance and wait two frames before execution so post-battle deferred reselection cannot override them; BattleModal now pings TutorialManager on battle completion so a step can advance when fights end; InfoModal close transitions emit `closed_info_modal`, which TutorialManager handles via `expected_action: "closed_info_modal"`; tutorial completion delegates to `GameManager.complete_tutorial_and_return_to_main_menu()`, finishes analytics as `won`, records tutorial upgrade completion for Food Card level 1, shows `CardModal` when newly unlocked, then returns to main menu.
		- ui_manager.gd: Central UI state with modal tracking and tooltip routing.

- F8 Screenshot: `GameManager._unhandled_input(KEY_F8)` calls `Utils.take_screenshot()` only when `debug_mode` is enabled. Names file by mode: `res://screenshots/screenshot_<scenario>.png` in scenario mode (from `GameManager.scenario_path`) or `res://screenshots/screenshot_<mapdata>.png` in custom mode (from `MapGenerator.data_file_path`). Camera centers/zooms out, UI hides for capture, then both restore.

- F8 Screenshot: `GameManager._unhandled_input(KEY_F8)` calls `Utils.take_screenshot()`. It centers and zooms out the camera, hides `UI`, captures the viewport, restores `UI` and camera, and saves to `res://screenshots/screenshot.png`.

- Camera Centering: Auto-centers on armies/assets. `CameraController.await_target_reached()` replaces arbitrary delays; `GameManager.ai_travel_to()` awaits camera arrival instead of timers. For human players: `GameManager._on_current_player_changed()` centers on first army or castle, but skips recentering while any battle is active to prevent mid-battle camera jumps.

## Data & Parameters
- Player.gd: resources, name/color/id, afford/pay helpers, serialization, plus a per-player `wealth_level` flag (poor/normal/rich) that TurnController updates at the end of each turn so recruitment logic can read each player's current tier, persistent AI-only `ai_castle_upgrade_savings_gold` (castle-upgrade funding), persistent AI-only `ai_raise_army_savings_gold` (carry-over raise reserve), and enemy intel memory (`enemy_army_memory` power/rounds, unlimited `enemy_army_composition_memory` with `turns_ago`, and round-limited garrison composition memory) used by InfoModal intel view.
- ResourceComposition.gd / ArmyComposition.gd: storage and math helpers; food cost, power.
- GameParameters.gd: unit stats and costs, recruitment caps and growth, movement costs, mining/search, castle defense, AI weights, pathfinder/AI constants, icons/scales, peasants-only recruitment parameters (AI_PEA_*), camera timing constants (CAMERA_*), configurable prebattle ranged volley count (`PREBATTLE_VOLLEY_ROUNDS`, currently `2`), optional attacker terrain-vigor penalties (`BATTLE_ATTACKER_TERRAIN_VIGOR_PENALTY_ENABLED` + per-terrain penalty map), optional ranged terrain hit reduction for archers/crossbowmen in forest/forest-hills (`BATTLE_RANGED_TERRAIN_PENALTY_*`), and the new wealth thresholds + composition tables used to pick poor/normal/rich army and garrison mixes. Current tuning: region recruit-cap percentages are L1..L5 = `4%/5%/6%/7%/8%`, constant population growth is `0.00`, generated starting garrisons get a `+2` baseline, and selected biome resource rolls use weighted outcomes (grassland food/forest wood/hills stone `1:45%`, `2:35%`, `3:20%`; forest-hills wood `1:65%`, `2:35%`; forest-hills stone `0:45%`, `1:35%`, `2:20%`).
- Enums (Enums/*.gd): Regions, Levels, Soldiers, Players, Resources, Unit traits, Castle types.
- Wealth scaling: GameParameters’ peasant ratios shrink by wealth tier (Normal halves, Rich zeroes) across army/garrison ideal compositions and the AI peasants-only min/target proportions so richer players favor pricier units automatically.

## Static vs Dynamic
- Static nodes: `Map/Regions`, `Map/Ocean`, `Map/Frame`, all UI nodes under `UI`, and `Players/Player1..6` containers.
- Dynamic nodes: `Region` instances under `Map/Regions` (land + ocean; ocean `Polygon` hidden, visuals from `Map/Ocean`), `Army` nodes parented to regions, `Castle` sprites under regions, border/overlay visuals.
- Border widths: `BorderManager` scales internal/ownership Line2D widths and ownership offsets from `MapGenerator.get_map_visual_scale()` (region-count profile).
- Icon scaling: map visual scale now resolves from region-count profile (`Utils.resolve_map_profile`) with anchor counts `tiny=137`, `small=264`, `medium=504`, `large=1040`, `huge=2104` and log interpolation between anchors; mountains still use aspect-ratio fit logic and only apply the shared Y offset scaling.
- RegionIconManager.gd: MapGenerator delegates all biome icon placement here; it randomizes hill/forest/hill_forest variants (`<biome>_1..3.png`), supports both `hill_forest` and `forest_hills` biome naming aliases for the same icon family, offsets standard icons, and picks the mountain sprite with the nearest proportions before fitting it inside the region; internal-mountain detection from `MapGenerator` is kept for potential scaling adjustments but currently applies a neutral 1.0 multiplier.
- Starting armies use `GameParameters.STARTING_ARMY_COMPOSITION_HUMAN`/`_AI` selected through `GameManager.get_starting_army_composition_for_player()` and passed into `Army.setup_army()`.
- Border widths and offsets: all runtime consumers (biome icons, castles, armies, movement arrows, and ownership borders) use the same region-count visual scale source from `MapGenerator`.
- Center markers: `MapGenerator.set_center_markers_enabled(true)` adds a `CenterMarkers` Node2D full of small red Polygon2D squares at each non-ocean region center (used by the GameManager debug toggle).
- Polygon centroids: Every time MapGenerator loads JSON or BorderManager rebuilds noisy polygons it recomputes the centroid for each region polygon (shoelace method with degenerate average fallback), stores it back into `region_by_id`, pushes it into `Region.center`, and writes it to each Polygon2D’s `center` metadata so castles/armies/icons/debug dots always anchor to the real geometric center instead of stale JSON values.
- Mountain adjacency: BorderManager skips drawing internal borders when both touching regions are mountains so mountain ranges remain uninterrupted visual blobs.
- Mountain icon layering: After every icon pass MapGenerator reorders all mountain sprites (marked via `mountain_icon` metadata) by their region center Y and assigns strictly increasing z-indexes so upper mountains render behind lower ones.

## Notable Relationships
- GameManager wires all managers, creates `TurnController`, and hands references around; UI/battle events flow back through `BattleManager` → `GameManager.finalize_battle_result`.
- Sound: `SoundManager` routes SFX through the `Sound` bus and music through the `Music` bus; Options and settings persist bus volume so all sound effects respect the same sound slider. Retreat horn (`sounds/retreat_horn.mp3`) plays via `SoundManager.play_retreat_horn()` whenever BattleModal shows a withdrawal message. Battle sound (`sounds/battle.wav`) uses the Sound bus volume, starts from a random position in the first half of the clip, and loops until stopped/faded.
- Sound players run with `PROCESS_MODE_ALWAYS` so opening the in-game menu (tree pause) does not stop music/SFX.
- Music playlists: `SoundManager` hosts `Playlist` (tracks + shuffle + repeat) and predefined `PLAYLIST_DEFS` with a tutorial playlist (`track2.mp3`, then `track1.mp3`, no shuffle, repeats) and a custom-map playlist (`track1.mp3`–`track7.mp3`, shuffle on, repeat). `GameManager` activates the tutorial playlist when tutorial mode is enabled and the custom-map playlist whenever game mode is custom; the music player advances through the playlist on the `AudioStreamPlayer.finished` signal, looping from the start.
- Wounded: Simple wounded system added. `GameParameters.WOUNDED_CHANCE` base chance; per casualty wounded chance is `base * (1 + defense%)`. `Utils.compute_wounded()` calculates wounded per unit type from loss dicts. Wounded are computed exactly once when a battle finishes (modal path computes on finish; background path sets it before finalization). `GameManager.finalize_battle_result()` never computes wounded; it assumes they exist on the report, allocates them proportionally (attackers: attacker army; defenders: across defending armies, garrison, and recruits), then applies losses. `Army` tracks a separate `wounded_composition` excluded from battles and excluded from food upkeep.
  - Storage: `Army.wounded_composition`, `Region.wounded_garrison`, `Region.wounded_recruits` (peasants only).
  - Allocation: Wounded distributed proportionally by availability before losses are applied. Defenders include armies, garrison, and recruits in the proportional distribution.
  - Summary: Battle summary displays total wounded from all sources; loss application removes from active comps/recruits.
- Healing: `GameParameters.CAMP_HEAL_CHANCE` controls healing probability. `Army.make_camp()` calls `heal_army(false)` to attempt healing across all wounded (per-unit chance). `Army.heal_army(force_at_least_one=false)` heals each wounded unit independently by chance; if `force_at_least_one` it guarantees at least one heal when wounded remain. There is no free post-battle heal on defeat or withdrawal; healing occurs only when making camp.
 - Healing: `GameParameters.CAMP_HEAL_CHANCE` controls army healing when camping. There is no free post-battle heal on defeat/withdrawal. At the start of a player's turn, all owned regions fully heal their regional wounded pools: `RegionManager.heal_wounded_for_player(player_id)` moves `Region.wounded_garrison` back into `garrison` and clears `Region.wounded_recruits` (peasants). This is invoked in `GameManager._process_player_turn_start()` before income.
- Ownership Overlays: Creation/update centralized in `MapGenerator`. Every non-ocean region always carries an `OwnershipOverlay`; neutral ownership keeps it fully transparent rather than removing it so highlight/tween logic can reuse it (existing tinted overlays are preserved).
  - `create_ownership_overlay(region_id, player_id)`: Ensures the overlay exists, syncs the noisy polygon, and applies the player tint (alpha 0.4).
  - `update_ownership_overlay(region_id, player_id)`: Recolors existing `OwnershipOverlay` without changing alpha; used when conquering non‑neutral regions.
  - `remove_ownership_overlay(region_id)`: Resets overlay color to transparent neutral black (keeps node alive for reuse).
  - `RegionManager.set_region_ownership` still calls create for previous owner = -1, update otherwise; neutralizing a region routes through `remove_ownership_overlay`.
 - InfoModal: Region view shows garrison totals/recruits plus garrison composition under Defenders; costs hide when the resource cost is 0.
 - InfoModal intel rendering is per-army card: only enemy/non-selectable armies are masked with `?`, while player-owned armies keep full stats even in neutral/enemy regions (used by SpawnEvent manual placement flow).
- TurnController owns pathfinder/scorer; calls `GameManager.ai_travel_to` for movement and battle stepping; uses `BudgetManager` and `RecruitmentManager` when armies request reinforcement.
- PlayerManagerNode is the authoritative per-player economy and emits player-change signals used by UI.
 - AI camera pacing: `GameManager` routes AI actions (castle placement, move steps, battle resolution) through `AICameraDirector` to enforce pre-action and post-action waits; camera snaps instantly to targets and all AI camera delays scale down with the AI Speed modal when set above Normal.

### army_manager.gd (ArmyManager)
- Purpose: Army creation, selection, and adjacent-region movement.
- Key: `create_army()`, `move_army()`, `move_army_to_region()`, `reset_all_army_movement_points()`.
- SpawnEvent support: `show_custom_target_arrows(target_ids, source_region_by_target)` and `clear_custom_target_arrows()` render the same arrow/highlight style for explicit (non-neighbor) placement targets.
- Capacity: `GameParameters.MAX_ARMIES_PER_REGION = 5` enforced on creation/movement helpers (`is_region_at_army_cap`, `get_armies_in_region`, `get_army_count_in_region`) so UI/AI respect the stack limit.
- Stacking: `_apply_army_offsets_for_region(region)` repositions all armies in a region to avoid overlap using fixed extra offsets. Applied on create, move (both source and target), and retreat. Pattern: 1st = base; 2nd = x-15/y-10/z-1; 3rd = x-30/y-20/z-2; 4th = x+15/y-10/z-3; 5th = x+30/y-20/z-4; 6th+ same as 1st.
- Ready highlight: Caches `_ready_highlight_player_id`, listens to `Army.movement_points_changed`, and refreshes `VisualManager.update_ready_army_highlights()` on turn start, move/camp MP spend, resets, and army removal.
- Retreat Support: `get_previous_region_for_army()` exposes the last region so cameras can focus on withdrawal destinations.
- Raised armies now spawn with 1 peasant token (set in `Army.setup_raised_army`) to avoid empty stacks on creation.
- Selection: `move_army()` preserves the active selection when the moved army was already selected (so the selected region updates to the new location after movement).
- Nearby cache events: exposes cache maintenance hooks (`on_army_created`, `on_army_moved`, `on_army_removed`, `on_region_castle_presence_changed`, `on_region_owner_changed`) and `debug_log_nearby_entities_for_region(region_id)` used by Ctrl-hover debug dumps.
- Fast entity lookup: `get_army_by_entity_id()` / `get_army_by_instance_id()` resolve armies via the central `army_by_entity_id` index, avoiding repeated full scans in AI turn logic.

### ai_camera_director.gd (AICameraDirector)
- Purpose: Coordinates AI camera focus and timing around the shared `CameraController`.
- Key: `await_focus_on_army()`, `await_focus_on_region()`, `await_delay()`.
- `GameManager` uses it to wrap AI castle placement, movement steps, and battle resolution with the configured pre/post waits.

## Missing/Added Since Last Map
- Extracted UI modals into separate `.tscn` scenes and instanced in `main.tscn` (previously described as inline).
- New/now-central: `TurnController.gd`, `BattleManager.gd`, `VisualManager.gd`, `BudgetManager.gd`, `BudgetComposition.gd`, `RecruitmentManager.gd`, `PlayerManagerNode.gd`.
- Translation sync utility: `translations.py` downloads a hardcoded Google Sheet tab (fixed URL + GID in script) and overwrites `translations/hotw_translations.csv` when run from command line.
- Steam upload utility: `steam_upload_build.sh` first exports release builds from Godot presets `windows` and `macos` into `pcexport/`, normalizes app bundle naming (`hornofthewarlord.app` -> `HornOfTheWarlord.app` when needed), ensures final artifact names are `HornOfTheWarlord.app` / `HornOfTheWarlord.exe` / `HornOfTheWarlord.pck`, copies them into Steam ContentBuilder content folders (`content/macos`, `content/windows`), then runs Steam build upload via `builder_osx/steamcmd.sh` and `../scripts/app_build_4694060.vdf`.
- Demo Steam upload utility: `steam_upload_demo.sh` mirrors the export/copy/upload flow for Demo and runs Steam build upload via `builder_osx/steamcmd.sh` and `../scripts/app_build_4787020.vdf` (depots `4787022` windows, `4787023` macOS).
- Final Steam upload utility: `steam_upload_final.sh` mirrors the export/copy/upload flow for the full game, clears stale generated export artifacts from `pcexport/` before exporting to avoid partial `.pck` write failures, and runs Steam build upload via `builder_osx/steamcmd.sh` and `../scripts/app_build_4380120.vdf` (depots `4380122` windows, `4380123` macOS; build output `output_release/`).
